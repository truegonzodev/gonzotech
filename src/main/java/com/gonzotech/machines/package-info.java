/**
 * Мультиблоки и функциональные блоки энергетики.
 * <p>
 * Фаза 2 — паровая ветка: топка → паровой котёл → генератор Стирлинга →
 * электропечь. Блоки не образуют «мультиблок» в классическом смысле: каждый
 * просто хочет видеть нужного соседа вплотную и передаёт ему ресурс напрямую
 * (провода появятся позже).
 *
 * <h2>Ресурсы</h2>
 * GTH (тепло) · Steam (пар, mB) · GTU («электричество») · Water (вода, mB).
 * Ёмкости и балансные числа — в {@link com.gonzotech.machines.energy.MachineDefs}.
 *
 * <h2>Подпакеты</h2>
 * <ul>
 *   <li>{@code energy} — глобальные определения, буфер ресурса, интерфейсы приёмников;</li>
 *   <li>{@code block} — блоки-машины и их BlockEntity + логика тика/передачи;</li>
 *   <li>{@code menu} — контейнерные меню (ПКМ → GUI);</li>
 *   <li>{@code client} — экраны (процедурный рендер шкал);</li>
 *   <li>{@code registry} — реестры блоков/предметов/BE/меню.</li>
 * </ul>
 */
package com.gonzotech.machines;
