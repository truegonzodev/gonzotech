# 🦝 Gonzo Tech

> Хардкорный научно-индустриальный мод про энергетику и астроинженерию для **Minecraft 1.21.4 (NeoForge)**.
> Единая вселенная прогрессии: **пар → тепловая энергетика → деление → кремний/литография → термояд → токамаки → ИИ → космическая инфраструктура → сферы Дайсона → антиматерия → чёрные дыры → квантовый вакуум → червоточины → божественный контроль над пространством.**

<sub>Living document · Репозиторий проекта `gonzotech` · Актуальный статус · По факту реализованы 2 из 16 эпох (паровая + атомная/тир-2), сверху — психика, радиация и «Заметки учёного»</sub>

---

## 🌍 Философия и Ключевой Принцип

1. **Физика мира детерминирована и едина на всех сидах:**
   - Температуры плавления, теплопроводность, КПД турбин и реакторов, радиация и свойства материалов — константы. Игрок один раз учит физический язык мода и может применять знания в любом мире (Thaumcraft-style).
2. **Сид мира меняет ТОЛЬКО замок исследования:**
   - Сид генерирует уникальный скелет уравнения на Зелёной Меловой Доске (какие переменные зафиксированы, какие скрыты вопросиками `?`).
   - Фундаментальные связи между величинами («любовь» и размерности) строго фиксированы.
3. **Качество формулы НЕ влияет на КПД машин:**
   - Единственная цель — достичь **`Баланс системы ≥ 90%`**. Элегантная формула из 3 узлов и «Франкенштейн» из 30 узлов одинаково разблокируют технологию.

---

## 🚦 Текущий статус реализации (Stance проекта)

| Модуль / Подсистема | Статус | Детали реализации |
|---|---|---|
| 🧮 **Chalkboard & Research** | ✅ Готово | 7D SI анализатор размерностей, Drag&Drop GUI меловой доски, Greedy-генератор **16 открытий**, серверная проверка формул (анти-фордж), сохранение прогресса игрока, книга заметок учёного (**41 страница** по 5 главам-эрам, вкладки + иллюстрации; наполнены глава I «Эпоха I» и глава «Познание» — отдельная книга по действиям игрока) |
| ⛏️ **Геология и Рудная база** | ✅ Готово | 26 руд, генерация жилами (stone/deepslate/nether) + замещение кальцита геод в кальциевую руду; бонусы по вмещающей породе (дробилка/центрифуга), а не по биомам, слитки, самородки, пыль, блоки; реактивный цесий (взрывы в воде) |
| ⚙️ **Паровая ветка (Открытие 1)** | ✅ Готово | Топка, Котёл, Стирлинг, Электропечь, Конденсатор, Насос, Аккумулятор, Генератор булыжника, буферы GTH/GTU/Steam/Water, прямоугольная паровая турбина (порты/компараторы/Smart CTM) и продвинутый парогенератор 5×5×5 |
| ⚛️ **Атомный пласт (Открытие 2, тир-2)** | ✅ Готово | Ядерная топка, дробилка, центрифуга ЦФ1УР, измельчитель, пресс, эл. печь II, помпа II, аккумулятор II, генератор булыжника II, завод сплавов, катка/индуктивный модуль/пуансоны/формы, тир-2 логистика, гейт botched_mechanism + книга рецептов |
| 🏭 **Завод Сплавов 5×5 + сплав** | ✅ Готово | Сетка 25 слитков → динамический сплав `custom_alloy` (состав в data component), каталог материалов (C/H, плавление, инертность…), взвешенный тинт, кирка/меч и полный набор брони из сплава, equipment-ассеты, зачарование через item-теги |
| ⚗️ **Переработка руды** | ✅ Готово | Дробилка (руда → 1× raw гарантированно + 3 вероятностных броска, бонусы по host-камню), центрифуга ЦФ1УР (промывка кипятком, 22 базовые строки, порошки + побочки), цепочки восстановления оксидов и плавки |
| 🔀 **Логистика труб и сетей** | ✅ Готово | 5 типов труб + универсальная жидкостная + универсальный узел (−10% throughput), пучки с послойным поворотом гаечным ключом, предметные фильтры и отсеиватели, компараторы, полный тир-2 набор труб/узлов |
| 🖥️ **GUI-система «слоёный пирог»** | ✅ Готово | 512×512 лист, слои фон/шкалы/дёрка, альфа-маска `GuiMask`, проявляющиеся текстуры-заливки, слоты 16×16 с гэпом 2px, ценники-тултипы, процентные тултипы прогресса у тир-2 |
| 🌌 **Космос и Измерения** | ✅ Готово | 8 измерений (`moon`, `mars`, `europa`, `solar_orbit`, `alpha_centauri_orbit`, `deep_space`, `blackhole_yx989_k2`, `blackhole_zangler_11`), Zero-G полёт (скорость `0.007F`), кастомный скайбокс |
| 🕳️ **Шейдер Чёрных Дыр (Гаргантюа)** | ✅ Готово | Релятивистское линзирование Шварцшильда (160+ FPS), аккреционный диск с эффектом Доплера, белое фотонное кольцо Эйнштейна, точный ISCO-зазор, гигантские 32×32px ванильные билборд-частицы пыли с динамическим остыванием и отсечением за тенью |
| 🧠 **Психика и Стресс (Gonzo)** | ✅ Готово | Три шкалы (зависимость / стресс / кризис) с HUD слева от хотбара; все источники и стоки по спеке; эффекты стресса (тремор, галлюцинации, дроп предмета, инфаркт «ровно 1 HP», слепота); кризис 38/56/67/74/87 — каскад-чекпойнт, сон с динамитом, микрошаг, полноэкранный оверлей, ложная смерть, взгляд мобов; фейк-экран смерти в ванильном виде; `/gonzotech debug psyche` |
| ☢️ **Радиация, УФ и химия** | ✅ Готово | Доза и 5 категорий с эффектами, единицы Zt (nZt…GZt), фон чанка и заражение, контур ×0.003, хазмат I (часть 5 % / сет 60 %, мягкая доза 20 mZt/с), некроз (вечный), «Зуд» I–III, УФ с триггерами, токсичность металлов «Tx/s», абсорбент + «Очищение», дозиметр / УФ-радиометр / телифон; препараты (Цистамин/Пентацин/ДТПА — применение ПКМ), некроз −2·(L+1) макс. HP (лечение — только полный курс ДТПА), зуд −20·L % natural regen |
| 🪐 **Сферы Дайсона и «Пекло»** | ⏳ Очередь | Кастомный рендер Дайсона в скайбоксе готов; таймер угасания Солнца и миссии «Икар» в бэклоге |
| 🔬 **Литография и Токамаки** | ⏳ Очередь | Контроллеры чипов, степпер, чистая комната, фото-кожух токамака |
| 🌀 **Губка Менгера и Червоточины** | ⏳ Очередь | Фрактальный эндгейм-реактор в глубоком вакууме, свободная форма, червоточины |

> 🧪 **Эпоха III — мозговой штурм:** [`docs/EPOCH3-BRAINSTORM-2026-09-22.md`](docs/EPOCH3-BRAINSTORM-2026-09-22.md)
> (материалы и «спящие» металлы, 7 семейств реакторов, литография как линия, а не ГУИ-мини-игра).
>
> 🏗 **Эпоха III — база:** [`docs/EPOCH3-BASE-2026-09-22.md`](docs/EPOCH3-BASE-2026-09-22.md)
> (контур из разных материалов, 9 машин базы, щипцы/ковш/ящики/тяжёлая дверь, чистая комната,
> литография 3×3 и три микросхемы, порядок волн) и
> [`docs/HEAVY-DOOR-2026-09-22.md`](docs/HEAVY-DOOR-2026-09-22.md) (как устроены ванильные
> двери и почему нашей двери 2×2 нужно 4 модели).
>
> 🗂 **Отложенное и запланированное — единый реестр: [`docs/DEFERRED-2026-09-22.md`](docs/DEFERRED-2026-09-22.md)**
> (режим «хардкор», стоки кризиса, ломка зависимости, план страниц Эпохи III, заделы в коде; ~~препараты~~ — закрыты пластом 24.09).
>
> 🗂 **Бэклог идей автора (22.09.2026, ночь):** пластинки (дабстеп / фолк-вестерн-блюз / мем-треки —
> ждём `.ogg`, вопрос по АП), одичавшие жители под землёй, **мобы-телифоны**, «польские альтушки»
> (`villager`-модель) в руинах заводов, робот «подай масло», **пивоварение и дистилляция (гейт 2–3
> Открытие)**, перевёрнутый револьвер и **28 достижений** (12 из списка + 16 по Открытиям) — дословно
> в `docs/UPDATED_TEMP_NOTES.md` §15.

---

## 🧱 Последний инженерный итог: паровая турбина и Smart CTM

- Паровая турбина собирается только как полный прямоугольный параллелепипед: оболочка из casing-блоков с допустимыми node-заменами и роторы во всём внутреннем объёме. Любая часть открывает общее GUI.
- Сервер сохраняет у casing только `formed`; информация о рамке, портах и углах не попадает в BlockState и не требует тысяч model variants.
- На клиенте одна Smart CTM baked-модель при пересборке render section рассчитывает восемь соседей каждой из шести граней. Затем она берёт неизменяемые quads из cache по ключу `(face, mask)` — соседей во время каждого кадра не сканирует.
- Каждая грань разбита без наложений на зоны `2 / 12 / 2`: base texture, 16×2 line, 2×2 outer corner и 2×2 inner corner. Правило `N + E + !NE` создаёт inner corner на диагональном casing вокруг node-отверстия.
- Для Smart CTM отключён ambient occlusion: составные texture quads больше не получают ложные прямоугольные AO-тени от соседнего полного блока. Коллизия и culling скрытых граней остаются обычными.

## 🚪 Двери Эпохи III («Открытие 3»): свинцовая, вольфрамовая, гермодверь

- **Что это:** `gonzotech:third_heavy_door_lead` — дверь 1 блок в ширину и 2 в высоту,
  **раздвижная**: закрытая — сплошная стенка (6 пикселей, «как стеклянная панель»), открытая —
  створка исчезает без анимации, остаётся рама, проём 12 px свободен. Открывается **и рукой (ПКМ),
  и редстоуном**.
- **Числа автора:** hardness `25`, взрывостойкость `8`, хитбокс — габарит модели (`6×16×16`),
  звук — ванильный железный (`IRON_DOOR_OPEN/CLOSE`) с питчем **0.3–0.5** («тяжелее» обычной
  железной). Тег `minecraft:mineable/pickaxe` — нужна кирка.
- **Открытие 3:** и показ рецепта в книге, и сам крафт гейтятся «Открытием 3» — рецепт
  крафтится в `botched_mechanism` до открытия (как тиры 1–2, см. `TierThreeCrafting`).
  По правилу «id = Открытие» id получил префикс `third_` (первый предмет третьего тира).
- **Рецепт (автор 22.09):** 3×3 — `свинец · свинцовый витраж · свинец` / `свинец · железная
  дверь · свинец` / `свинец · свинцовый витраж · свинец`.
- **Защита от радиации:** как у свинца — `0.02` (правки одной строкой в `RadMaterials`); автор:
  «сделаем разные двери — отличие в одном параметре защиты от радиации».
- **Пара блоков** (`half=lower/upper`) связана как у ванильной двери: соглашение об адресе,
  зеркалирование через `updateShape`, редстоун читается у обеих половин, двойного дропа нет.
  BlockEntity нет вовсе. Разбор ванильного механизма — `docs/HEAVY-DOOR-2026-09-22.md`.
- **Контур радиации:** дверь — в теге `gonzotech:contour_seal`. Закрытая **замыкает контур и
  входит в среднее своим фактором (0.02)**; открытая — честная дырка (1.0). Ванильная железная
  дверь тоже в теге: замыкает контур материалом 0.5 — «гадит контур», как и сказал автор.
- **Модели:** 4 файла (`down/up` × `closed/opened`) в `models/block/third/`, две текстуры
  `heavy_door_lead_down_closed.png` / `_up_closed.png` (64×64 UV-раскладки автора, плейсхолдеры:
  одна текстура обслуживает оба состояния — это задумка, не ошибка).
- **Три двери** (автор 22.09) — одна механика, отличие в одном параметре (защита от радиации)
  и питче звука:

| дверь | id | рецепт 3×3 | защита | питч |
|---|---|---|---|---|
| Свинцовая | `third_heavy_door_lead` | свинец · свинцовый витраж · свинец / свинец · железная дверь · свинец / то же | **0.02** | 0.3–0.5 |
| Вольфрамовая | `third_heavy_door_tungsten` | вольфрам · борное стекло · вольфрам / вольфрам · железная дверь · вольфрам / то же | **0.003** | 0.1–0.3 |
| Гермодверь | `third_hermetic_door` | алюминий · целлулоид · алюминий / железо · железная дверь · железо / железо ×3 | **0.33** | 1.2–1.4 |

Гермодверь — **для чистой комнаты** (защита от фона слабая, зато годится для чистого контура);
её рецепт требует **целлулоид**, который пока не крафтится — то есть гермодверь закрыта до
химического завода (так и задумано). Питч — параметр конструктора `HeavyDoorBlock`,
а не константа.

## 🧪 Полимеры (Эпоха III)

Пять предметов во вкладке «Компоненты»: **полиэтилен**, **поливинилхлорид (ПВХ)**, **целлулоид**,
**резина**, **смола**. **Рецепты есть** — химический завод, цепочки №3–8 (смола ↔ ванильный
`resin_clump`, реверсив). Применений пока немного: **целлулоид** идёт в рецепт гермодвери,
остальные — задел под пластмассы Эпохи III. Иконки — плейсхолдеры.

## ⚠️ Совместимость миров (21.09.2026)

Id механизмов приведены к правилу «префикс = Открытие» (`electric_furnace` → `first_electric_furnace`,
`crusher` → `second_crusher` и т. д. — полная таблица в `docs/AUDIT-2026-09-21.md`, §9.12).
Из-за смены id **миры и схемы, созданные раньше, несовместимы** (старые постройки станут
«неизвестными блоками»). Правило и сверка — `docs/UPDATED_TEMP_NOTES.md` §0, `audit/naming_scan.py`.

---

## 🔥 Продвинутый парогенератор (многоблок 5×5×5)

- **Форма фиксирована**: ровно 5×5×5. Внешний слой — блоки `second_steamgen_casing` (с допустимыми node-заменами на порты), внутренность 3×3×3 — ядра `second_steamgen_core` и/или драгоценные блоки-теплообменники. Контроллер — ядро в углу `(minX+1, minY+1, minZ+1)`. Любая часть открывает общее GUI.
- **Порты (минимум по одному каждого вида)**: GTH-вход (тепловая/универсальная нода), вода (водная/универсальная жидкостная/универсальная нода), пар (паровая/универсальная жидкостная/универсальная нода).
- **Конверсия**: за одно «событие варки» тратится `15 mB воды + 11 GTH` и вырабатывается `12 mB пара × M`. Машина каждый тик делает максимум событий, что позволяют потолок выработки, запасы воды/GTH и место в баке пара. Остатки событий и пара косятся в milli, поэтому малые мощности не «застревают».
- **Множитель теплообменников** `M = 1 + E_avg·(1 + 0.1·(n−1))`, где `E_avg = (C+H)_{avg}/200` по всем `n` теплообменникам. Ядра задают throughput (`34 mB/т` базово на ядро, ДО множителя), теплообменники — только эффективность: на единицу пара уходит `15/(12M)` mB воды и `11/(12M)` GTH.
- **Базы на ядро** (линейно от числа ядер): вода `488 mB`, пар `1526 mB`, GTH `4096`, пропускная способность жидкостей `128 mB/т`.
- **Паразитика**: пассивное остывание `2 GTH/t` и утечка `1 mB` пара/т — независимо от нагрузки (только пока сформирован и ресурс есть).
- **Теплообменники** — любые металлические блоки мода, значения C/H берутся у материала-хоста из `AlloyMaterialCatalog` (например, платина: C 75 + H 75 = 150 → E 0.75). Ванильные блоки подключаются явным списком в `SteamGenHeatExchangers.VANILLA_EXCHANGERS`.
- **GUI**: три шкалы — GTH, вода, пар (раскладка 1:1 с котлом). Тултипы показывают ёмкости, фактические потоки приёма/отдачи, число ядер и бонус теплообменников.
- **Сборка**: Smart CTM, как у турбины. Сервер сохраняет у блоков только `formed` (+`controller` у ядра); порты, ядра и теплообменники — в NBT контроллера.
- **Прогрессия**: пароген — машина Открытия 2. Крафт корпуса/ядра до Открытия 2 даёт `botched_mechanism`; в книге рецептов и в витрине второго открытия (креатив-вкладка) — только после. Рецепты: корпус — стальная пластина/железо/железо, прутья/чугун/прутья, железо/железо/стальная пластина; ядро — чугун/железный люк/прутья, пластина/котёл/пластина, конденсатор/железный люк/чугун.

## 🏭 Атомный пласт: тир-2 машины (Открытие 2)

Полный второй тир промышленной линейки; все машины — через `TierTwoCrafting`:
рецепты в книге появляются после Открытия 2, а крафт до открытия тратит
ингредиенты и выдаёт бесполезные `botched_mechanism`:

- **Дробилка** (тир-2): вход — блоки руды (гонзо-руды в 4 host-вариантах + 20 ванильных), выход — гарантированный primary (`raw_*` у гонзо, слиток/самоцвет у ванильных) + 3 броска, зависящих от host-камня (stone/deepslate/nether/calcite), а не от металла; 13 простых камней (гранит/базальт/незеррак/туф/…) — отдельная таблица `ROCK_RECIPES`.
- **Центрифуга ЦФ1УР** (тир-2): промывка `raw_*` настоящим кипятком (`hot_water` буфер) — главный порошок + до 3 вероятностных побочек по 22 базовым строкам; выпуск `cesium_dust` взрывается (см. «Вода и реактивный цесий»).
- **Измельчитель** (тир-2): строгие 1-в-1 конверсии порошка в слиток (все металлы каталога с dust + ванильные железо/медь; йод/сера/ртуть намеренно без вывода).
- **Пресс** (тир-2): пластины и провода; форма и пуансон — селекторы режима, а не ингредиенты; ровно четыре валидные комбинации, клиновидный пуансон требует пустой формы.
- **Электропечь II** (тир-2): две независимые переплавочные линии (по слоту сырьё/выдача + своя стрелка прогресса и свой «Переплавка: N%» тултип).
- **Помпа II / Аккумулятор II / Генератор булыжника II** (тир-2): второй тир буферов и добычи; булыжник II работает только на ведре лавы (без слота кирки) и имеет процентный тултип «Генерация: N%».
- **Завод сплавов** (тир-2): 5×5 сетка входных слитков → один выходной слот; сервер агрегирует весь состав в динамический `custom_alloy` (см. ниже).
- **Ядерная топка** (тир-2): см. раздел ниже.
- **Компоненты тира**: `coil`, `inductive_module`, `wedge_punch`, `flat_punch`, `ingot_form`, `plate_form`, `core_form`.
- **Логистика II**: полный набор вторых труб/узлов (wire/heat/water/steam/item/universal + узлы), второй фильтр и отсеиватель.

**Крафтовые гейты двух уровней:**
- *Открытие 1* (`Phase3Events`): эл. печь + ВСЯ логистика первого тира + помпа/аккумулятор/генератор булыжника + части паровой турбины физически «закрыты» (`botched_mechanism`). Исключение — гаечный ключ крафтится всегда. Котёл/топка/стирлинг/конденсатор крафятся до открытия, их рецепт лишь скрыт в книге.
- *Открытие 2* (`TierTwoCrafting`): весь атомный пласт выше.
- *Книга рецептов* (`RecipeUnlocks`): tier 1 — машины/логистика Открытия 1 + стройматериалы II; tier 2 — пылевые сплавы верстака; tier 6 — сверхплотный лёд; отдельно: 3 рецепта после 20 минут наигранных в мире и «всегда» (доска, псевдокатушка, заметки, botched_mechanism).

## 🧪 Система сплавов (custom_alloy)

- **Завод сплавов** принимает 25 произвольных слитков (`AlloyFoundryRecipes`), прогресс плавления — отдельная шкала; результат — один предмет `gonzotech:custom_alloy` с составом в data component (доли металлов), а не отдельными предметами на пропорцию.
- **Свойства** считаются по весам из `AlloyMaterialCatalog`/`AlloyProperties`: прочность, температура плавления, твёрдость, проводимость, радиозащита, **инертность** и пр. Именные пресеты (чугун, сталь, нержавейка, кортен…) — каталогизированные составы.
- **Тинт**: взвешенный цвет состава окрашивает предмет (клиентский тинт-источник `AlloyTintSource`/`AlloyClient`), включая одежду.
- **Снаряжение**: кирка и меч из сплава; полный набор брони (нагрудник/шлем/поножи/ботинки) — крафт из одного точного состава, статы ×множитель слота (×1.0 / ×0.5 / ×0.7 / ×0.4 на ВСЕ статы включая прочность). Одетая броня рендерится через equipment-ассет `assets/gonzotech/equipment/custom_alloy.json` (1.21.4-канон `entity/equipment/humanoid{,_leggings}`), оба листа дизейбл-окрашиваемы.
- **Зачарование**: меч/кирка/броня в ванильных item-тегах `#minecraft:enchantable/*`; сплавы с инертностью ≥ 50 незачаровываемы (компонент ENCHANTABLE не ставится).

## ⚗️ Вода и реактивный цесий

Контакт с водой опасен для поллуцита (`raw_cesium`), цезиевых ore BlockItem-ов, слитка, самородка, пыли и BlockItem цезиевого блока-хранилища. В инвентаре игрока в воде сохраняется установленный ритм: взрыв силой 1 каждые 8 тиков. Выброшенный в воду реактивный stack взрывается один раз, расходуется и не создаёт бесконечную цепочку тиков.

ЦФ1УР не является безопасным обходом: её операция использует настоящий кипяток, поэтому выпуск `cesium_dust` вызывает тот же разрушающий взрыв силы 2, что и печи. Добавлены furnace и blast-furnace recipes `cesium_dust → cesium_ingot`; обе печные ветки уже проверяют произведённый `cesium_ingot`, поэтому также взрываются. Дробилка намеренно не использует этот side effect.

## Ядерная топка, кориум и вольфрамовый абсорбер

- **Ядерная топка** (Discovery 2): один слот топлива, 8 видов уран/ториевого топлива (слитки, самородки, блоки, необработанные уранинит/торианит), горит с постоянной генерацией 262 GTH/t, отдача до 488 GTH/t, буфер 68 408 GTH, пассивная потеря 0,2 GTH/t. Горячий буфер сокращает время горения топлива до −25% (линейно от 22 040 GTH). Крафт: паровая топка по центру, **свинцовый блок** слева, **вольфрамовый слиток** справа, рамка из 4 свинцовых слитков и 4 стальных пластин; в креатив-вкладке машина лежит в самом конце списка.
- **Горение**: вертикальная шкала burnout между слотами; тултип «Горение: осталось X%» (процент остатка, не секунды).
- **Три полосы компаратора:** `0` → `1` при >50 000 GTH (огненный риск: искры вокруг и поджог стоящего игрока) → `15` при >64 000 GTH (критический).
- **Вероятностные пожароопасные эффекты** (объём 3×3×3 вокруг машины, один бросок раз в секунду):
  - **>50 000 GTH** — 5% шанс, что на одном случайном горючем блоке разгорится огонь;
  - **топка, >64 000 GTH** — 3% шанс, что один случайный незащищённый блок плавится в **расплавленный кориум**. Первые 20 секунд после преодоления 64 000 GTH сама топка защищена; если GTH держится выше порога без сброса (любое падение ниже 64 000 заново запускает таймер), топка **сама становится кандидатом** и может расплавиться в источник кориума;
  - **вольфрамовый абсорбер, >94 000 GTH** — 3% шанс, что один случайный незащищённый блок плавится в **лаву**.
- **Расплавленный кориум** — лаво-подобная жидкость (`molten_corium` / `flowing_molten_corium`), зачерпывается ведром кориума и разливается источником. Поведение как у лавы: **жжёт и поджигает сущности** (урон от лавы, 15 с горения), **поджигает горючие блоки**, кислорода не требует. Реакция с водой повторяет схему ванильной лавы: **источник + вода → блок кориума** (застывший, аналог обсидиана), **поток + вода → `dead_stone`**, **распространение вниз в воду → `dead_stone`**. Текстуры — собственные анимированные (`still` 16×128, 8 кадров; `flow` 32×128, 2×8 кадров).
- **Защищённые от плавления блоки:** обсидиан, плачущий/карминный обсидиан, bedrock, barrier, командные/структурные блоки, **долговечный бетон**, сверхплотный лёд, вольфрам, VR-20, стеллит; а также воздух/пещерный воздух/пустотный воздух/структурная пустота.
- **Вольфрамовый блок** — пассивный абсорбер со скрытым буфером 112 000 GTH: принимает тепло (до 488 GTH/t) только через примыкающие HEAT-трубы/узлы, рассеивает 66 GTH/t + **32 GTH/t за каждый примыкающий блок сверхплотного льда**; >50 000 GTH — искры, >94 000 GTH — плавление блоков в лаву. HUD гаечного ключа показывает уровень нагрева.
- **Сверхплотный лёд:** 8 синего льда + 1 стеллитовый блок. Крафтится всегда; в книге рецептов появляется с **Открытием 6** (Токамак).

## 🖥️ GUI-система «слоёный пирог»

Все машины используют общую клиентскую базу `MachineScreen` под рисованный PNG:

- **Лист** — единый PNG 512×512 (`textures/gui/<machine>_gui{_bg}.png`), окно 176×166 живёт в центре листа (блит от `(128,128)`); рисунок может торчать за окно до 128 px во все стороны. Окно в арте — 176×167 с левого верхнего угла `(128,127)`.
- **Слои (сверху вниз):** Z3 предметы+тултипы (ванила) → Z1 передний PNG с дырками → Z0 проявляющиеся шкалы → Z−1 задний PNG.
- **Альфа-маска `GuiMask`** строится из переднего PNG (порог альфы ≤ 8/255 = «дырка»): заливка шкалы физически не может вылезти под непрозрачный рисунок — она клипится по наибольшей открытой подпрямоугольной области. Маска пересобирается под реальный размер окна (нестандартные высоты: завод сплавов, фильтр предметов).
- **Шкалы** — «проявление» текстуры без растяжения: `drawVBarTex` (вертикальная, тайл 16×16, привязка к низу — рост без сдвига паттерна), `drawHBarTex` (горизонтальная, слева направо), `drawHBarTexFull` (единая не тайлящаяся текстура, напр. 68×16 прогресс генераторов булыжника).
- **Слоты** — 16×16 физических, гэп 2px; раскладка в меню, позиция = левый верхний угол физического слота; `quickMoveStack` индексный, поэтому переносы раскладки безопасны.
- **Тултипы**: ценники ресурсов (GTH/GTU/вода/пар/кипяток) — у всех мехов; **процентные тултипы прогресса («Дробление: N%», «Генерация: N%», «Переплавка: N%», «Измельчение: N%», «Сплавление: N%», «Усталость») — только у тир-2 мехов**; у паровых тир-1 на прогресс-шкалах тултипов нет.

## 🌌 Финальные параметры Чёрных Дыр

### 1. `blackhole_yx989_k2`
- **Горизонт событий ($r_s$):** `120.0` блоков
- **Фотонное кольцо (Photon Ring):** `126.0 - 134.0` блоков
- **ISCO-зазор (Plunge Gap):** `120.0 - 135.0` блоков
- **Аккреционный диск ($R_{in} - R_{out}$):** `138.0 - 500.0` блоков
- **Орбитальные частицы:** `138.0 - 500.0` блоков, скорость `20.0..0.5` б/с, размер `12.0..38.4` блоков

### 2. `blackhole_zangler_11`
- **Горизонт событий ($r_s$):** `200.0` блоков
- **Фотонное кольцо (Photon Ring):** `214.0 - 224.0` блоков
- **ISCO-зазор (Plunge Gap):** `200.0 - 225.0` блоков
- **Аккреционный диск ($R_{in} - R_{out}$):** `228.0 - 1300.0` блоков
- **Орбитальные частицы:** `228.0 - 1300.0` блоков, скорость `20.0..0.5` б/с, размер `12.0..38.4` блоков
