# Отложенное: единый источник правды

**Дата свода:** 22.09.2026 (закрытие фазы «Открытия 1–2: психика, радиация, ГОСТ»)
**Ревизия:** ветка `arena/01a0c4a5-gonzotech`, `206c6a7` + докоммит этой сводки.
**Правило автора:** ничего отсюда НЕ удаляем — вычёркиваем по мере реализации.

> **Это единственный реестр отложенного.** Раньше «отложенное» жило россыпью: §16 заметок,
> §9.30 аудита, §6 отчёта по ГОСТу, вопросы в конце плана Эпохи III. Теперь всё сведено сюда;
> §16 заметок стал указателем на этот файл (чтобы не было двух источников правды).
>
> Смежные документы: полный бэклог идей автора — `UPDATED_TEMP_NOTES.md` §15 ·
> история правок фазы — `AUDIT-2026-09-21.md` §9 · план страниц Эпохи III —
> `NOTES-EPOCH3-PLAN-2026-09-22.md` · форматы единиц — `UNITS-GOST-2026-09-22.md` ·
> **мозговой штурм Эпохи III (материалы/реакторы/литография) — `EPOCH3-BRAINSTORM-2026-09-22.md`**.

---

## 0. Содержание

| Раздел | О чём |
|---|---|
| 1 | **Новое:** режим «хардкор» (два правила) |
| 2 | Запланировано автором — начинать только по слову |
| 3 | Ждёт решения автора (числа/выбор, без слова не начинаю) |
| 4 | Что осталось по каждой шкале |
| 5 | Заделы в коде (НЕ удалять) |
| 6 | Бэклог-указатели (пластинки, мобы, достижения, пивоварение) |
| 7 | Мои наблюдения по балансу (числа по текущим константам) |
| 8 | Открытые вопросы к автору (сводно) |
| 9 | Закрыто за эту фазу (история, чтобы не искать в аудите) |

---

## 1. НОВОЕ (22.09.2026, ночь): режим «хардкор»

Записано дословно: **«в режиме "хардкор" багровый день каждый день, + ни одна из шкал
не отнимается смертью»**. Реализацию не начинали — это запись на будущее.

### 1.1 Багровый день каждый день

- Сейчас: суневент идёт по расписанию `SunEventData.nextEventDay` / `lastEventDay`, счётчик
  докручивается раз в новый ванильный день (`SunEventServer`).
- В хардкоре: следующий багровый день = КАЖДЫЙ день (то есть багровый день перманентно, без «окон»).
- Место правки: `sunevent/SunEventServer` (пересчёт `nextEventDay`) + `SunEventData`
  (там же состояние Солнца и `snowWindowVanillaDay`). Проверка режима — `level.getLevelData().isHardcore()`.
- Смежное, что придётся решить: остаются ли обычные дни вообще (снег/монстры/горение в полдень —
  `SunEventServer.monsterNightNow`), и что с достижениями/заметками, которые ждут «первого суневента».

### 1.2 Смерть не отнимает ни одну шкалу

Сейчас смерть забирает/чистит (все точки — по коду):

| шкала | что делает смерть сейчас | где |
|---|---|---|
| Облучение (доза) | сбрасывается в ноль | `RadiationSystem.onPlayerClone` |
| УФ | чистится полностью | `PsycheStress.onPlayerClone` → `PsycheUltraviolet.clear` |
| Химическое заражение | −30 % от текущего | `PsycheChemical.onDeath` |
| Зависимость | −5 % от текущего | `PsycheStress.onPlayerClone` |
| Стресс | −5 % от текущего | `PsycheStress.onPlayerClone` |
| Кризис | не отнимается, наоборот **+500** | `PsycheStress.onPlayerClone` |

- В хардкоре: **ни одна из шкал не уменьшается смертью** (доза и УФ НЕ сбрасываются,
  зависимость/стресс/химия не чистятся).
- **Уточнение к автору:** кризис сейчас не уменьшается, а ПРИБАВЛЯЕТ +500. В хардкоре этот
  «штраф» остаётся или тоже снимается? (по умолчанию оставлю: кризис не отнимается — правило
  соблюдено, а +500 это не «отнятие»).
- Точки правки: `PsycheStress.onPlayerClone` и `PsycheStress.onDebugDeath` (отладочный триггер
  смерти обязан вести себя так же, иначе тесты разойдутся с игрой), `RadiationSystem.onPlayerClone`,
  `PsycheUltraviolet.clear`, `PsycheChemical.onDeath`.

---

## 2. Запланировано автором — НЕ начинать без слова

| # | механика | где спека | статус |
|---|---|---|---|
| 1 | **Массажные ванны** — сток стресса (вода + GTU + время) | заметки §8.4 | не начато |
| 2 | **Благовония** — стоки/аура «поставил и забыл» | заметки §8.4 | не начато |
| 3 | **Препараты** (Цистамин / Пентацин / ДТПА) — лечение радиации, заодно кризис и некроз | заметки §11.1, аудит §9.17, база §2.1 | состав утверждён на 100 %; 22.09 автор привязал их к **химзаводу** («даст доступ к препаратам, поднимем рецепты ЭДТА»); предложен механизм применения — **ампулы через наполнитель** (база §2.3), ждёт подтверждения |
| 4 | **Медитация** (в том числе в космосе, на МКС, на орбите ЧД) — вероятно, главный сток кризиса | заметки §8.4 | не начато |
| 5 | **Револьвер** → перевёрнутый револьвер (дуло в камеру, сброс кризиса) | заметки §15.3 | спека уточнена автором, не начато |
| 6 | **Антистресс** (сквиш, спиннер) — родня рычагу-фиджету | заметки §8.4 | не начато |
| 7 | **Пивоварение и дистилляция** → уточнено автором 22.09: **«веха сусловарения»** — чан (котёл) → бродильный бак → дистиллятор → **ректификатор отдельным блоком** | заметки §15.6, база §2.6 | гейт после 2–3 «Открытия» — точное число за автором |
| 8 | **Пластинки** (дабстеп / фолк-вестерн-блюз / мем-треки) | заметки §15.1 | ждут `.ogg` от автора, вопрос по АП |
| 9 | **Стоки кризиса** и «мощные евенты сброса» | заметки §8.4, аудит §9.30 | автор: «нужны как можно раньше»; рекомендовано начать с черновика чисел |
| 10 | **Ломка зависимости** (эффекты при «коридоре», когда сусло не пито) | заметки §8.3 | не начато |

---

## 3. Ждёт решения автора (вопрос без ответа — не начинаю)

1. **Цена подсказки доски** в стрессе/кризисе (сейчас подсказка бесплатная; врезка готова в
   `chalkboard/ResonanceClue.give`). Автор: «потом, запомнили уже записано».
2. **6 достижений без нового контента** — «Выкуси» (`SunState.GONE`), «Выкуси нахрен» (`BLACKHOLE`),
   «Слон в посудной лавке» (`MoltenCorium`), «Счастье» (зависимость 100 %), «Босс KFC»
   (`FatPetLogic.MAX_FATNESS` = ×2.3), «Ферментация» (`FERMENT_DELAY_TICKS`). Всё уже есть в коде —
   нужен только JSON достижения + lang. Не начато.
3. **Страницы Эпохи III** — 4 вопроса в `NOTES-EPOCH3-PLAN-2026-09-22.md` §12 (стиль от лица Гонзо
   или справочник; разбивка; иллюстрации; чем открывать — флаг события или «Открытие»).
3-a. **Эпоха III начата обсуждением (22.09.2026):** мозговой штурм по материалам, реакторам и
   литографии — `EPOCH3-BRAINSTORM-2026-09-22.md`; там же 10 вопросов автору (с чего начинаем,
   ТВЭЛы, число семейств реакторов, компактность линии литографии, обязательность процессоров,
   графит, жестокость аварий). Ничего не начинаем до ответов.
3-b. **База Эпохи III (22.09.2026):** автор дал состав базы (литография, химзавод, дистиллятор,
   бродильный бак, ПЦФОЗ, наполнитель, закатной станок, химический реактор, обдуватель) — разбор
   в `EPOCH3-BASE-2026-09-22.md` §0.1 (таблица «вопрос → ответ → статус»). **Ответы получены:**
   слои умножаются и толщина работает (✅ реализовано, `Containment` 2.0); дверь-заслонка в расчёте
   не участвует (✅ реализовано через тег `contour_seal`); гермоввод = буфер из двух гермодверь с
   очистителями, отдельного блока нет; чистая комната — **через эффект** (описание за автором);
   ЭДЛГ = ЭДТА (опечатка); ПЦФОЗ — отдельный станок; «база» — новый узел 3×3 из новых блоков,
   станок — **многоблок 3×3×2 через CTM**; грязь даёт **больше брака**; химзавод vs химреактор —
   «надо смотреть как идёт»; водка ↔ психика — «позже».
   **Осталось открытым:** (1) арифметика щипцов: −60 % (→ 26 из 64) или «64 фонят как 19» (−70 %)?
   (2) ковш: «для жидкостей, а также ртути, цезия (ковш не берёт цезий и ртуть)» — противоречие;
   (3) 10 вопросов по двери — `HEAVY-DOOR-2026-09-22.md` §8 (схема раздвига, число моделей,
   открывание рукой, толщина, прочность, железная дверь в теге, карманы, плейсхолдеры);
   (4) станок 3×3×2: сколько типов блоков базы и какие три микросхемы (литография);
   (5) каким эффектом будет чистая комната (ждём описания); (6) химзавод/химреактор — решается
   по ходу.
4. **Баланс УФ**: полная шкала = 20 ударов по 1 HP за 5 секунд (+~33 % шанс слабости/замедления,
   ~9.5 % шанс вечного некроза) — «не добивать», реже распад или урон по нарастающей?
5. **Достижимость химии**: порог зуда 38 % требует ~17 ртутных слитков одновременно (распад
   «0.3 % от текущего» выводит шкалу на равновесие). Поднимать пресеты, считать контакт с блоками?
6. **Токсичность блоков в мире** не считается — `ItemToxicity` читает только инвентарь: стоять в
   мёртвой жиже или рядом с бочкой ртути безопасно. Если задумано «жижа травит» — нужен контактный
   источник (блок под ногами / радиус, раз в секунду).
7. **Мёртвая жижа из дезоляции не фонит**: ворлдген-блоки не проходят через
   `BlockEvent.EntityPlaceEvent`, а чанк-скан учитывает только урановые руды. При 0.2 mZt/с это
   мелочь, но «озёра после дезоляции» радиоактивности не дают — нужен оффлайн-скан по чанку?
8. **Пункты 4, 5 и 7** авторского списка №3 (бэклог, заметки §15) — они были или вычеркнуты?
9. **«АП»** — что это (по контексту — авторские права на треки)?
10. **«Паровой сборщик»** (тир-1 после Открытия 1) — правило крафта не придумано (заметки §7).
11. **Переименования id → старые миры**: нужен ли переходный алиас (заметки §0)?
12. **`solar_panel` → `fourth_solar_panel`** и гейт тира 4 — только вместе с эпохой 4 (заметки §12.1).
13. **Хардкор, уточнение:** остаётся ли «+500 кризиса за смерть» (см. §1.2).

---

## 4. Что осталось по каждой шкале

| шкала | сделано | отложено |
|---|---|---|
| **Облучение** | категории 5/20/50/80/100, эффекты и вспышки, лечение, некроз, хазмат I (60 %/20 mZt/с), абсорбент и «Очищение», чанк-фон, дозиметр | рецепты приборов (ветка 3), очистные сооружения (после 3-й вехи); препараты (§2.3); `RadDose.damageSource()` — задел под препараты, сегодня не вызывается |
| **УФ** | механика целиком: таяние 1 %/тик от максимума, триггеры каждые 5 %, «переключаемость», источник от радиации >70 %, смерть чистит | **остальные источники: космос, токамак и т.д.** (точка входа `PsycheUltraviolet.charge(...)` готова); рецепт УФ-радиометра — ветка 3 |
| **Химическое заражение** | шкала, пресеты токсичности металлов, NBT-тег, «Зуд» I–III, бонусы к стрессу/дозе, смерть −30 % | препараты-очистители («в будущем помогут»); токсичность от блоков в мире (§3.6) |
| **Стресс** | все источники и стоки по спеке, эффекты (тремор, галлюцинации, дроп, инфаркт, слепота), бонус зависимости и зуда, чат-подсказки | стоки-механики из §2 (ванны, благовония, медитация, антистресс) |
| **Зависимость** | источники, распад после 20 дней, «коридор сусла», HUD, кризис от зависимости на 60–80 % | ломка — заход по эффектам зависимости (§2.10) |
| **Кризис** | каскад-чекпойнт (38 %), подмена предмета (56 %), сон с динамитом, микрошаг (67 %), экранный эффект (74 %), ложная смерть (87 %), взгляд мобов | **стоки кризиса** и «мощные евенты сброса» (§2.9) |

---

## 5. Заделы в коде (НЕ удалять)

- `RadDose.damageSource()` — тип урона для препаратов, сегодня не вызывается;
- `Necrosis.cure(player)` — снятие некроза, вызывается только вручную (препараты/команда — потом);
- `PsycheUltraviolet.charge(player, permille)` — вход для будущих источников УФ;
- `ItemToxicity.TAG_TOXICITY` — NBT-параметр для предметов со своей токсичностью;
- `ModItems.UV_METER` — предмет и HUD рабочие, рецепта нет (ждёт ветку 3);
- `RESTORE_FAKE_TIME = false` — откат времени мира в каскаде (автор: не откатывать);
- свои ключи `gui.gonzotech.crisis.death_*` — заменены ванильными, оставлены как задел;
- `ChalkboardClueClient.clear()` — задел под смену задачи при новой подсказке;
- `ResonanceClue.give` — единственная точка входа подсказки (её позовут револьвер и водка).

---

## 6. Бэклог-указатели

- **Идеи автора дословно** — `UPDATED_TEMP_NOTES.md` §15: пластинки (§15.1), фракции и мобы
  (§15.2: одичавшие жители, «польские альтушки» на villager-модели, робот «подай масло»,
  телифоны-мобы), перевёрнутый револьвер (§15.3), **28 достижений** (§15.4–15.5),
  пивоварение и дистилляция (§15.6).
- **План страниц Эпохи III** — `NOTES-EPOCH3-PLAN-2026-09-22.md` (7 обязательных блоков,
  28 страниц-кандидатов, черновик первой волны из 8 страниц).
- **База Эпохи III** — `EPOCH3-BASE-2026-09-22.md` (контур 2.0, 9 машин, щипцы/ковш/ящики/дверь,
  чистая комната, литография 3×3 и 3 микросхемы, порядок 10 волн).
- **Тяжёлая дверь 2×2** — `HEAVY-DOOR-2026-09-22.md` (разбор ванильных дверей по исходникам:
  два блока по соглашению `HALF`, `updateShape`, `BlockSetType`; почему «модели 1×2» не бывает;
  сколько моделей нужно; архитектура 4 блоков; ассеты и Blockbench; связь с контуром).
- **Реализовано 22.09 (см. аудит §9.40):** `radiation/Containment.java` — Containment 2.0 (слои
  перемножаются, толщина до 8 клеток, заслонки-двери не в расчёте, порог 10⁻³ → 0, `isEnclosed`).

---

## 7. Наблюдения по балансу (числа — по текущим константам, не предложения)

1. **УФ:** 20 триггеров на полную шкалу = 20 HP; в выживании без брони это почти наверняка смерть.
2. **Химия:** равновесие «приток = 0.3 %/с от текущего»: ртутный слиток — 2.2 %, палладиевый —
   0.5 %; зуд (38 %) требует ~17 ртутных слитков одновременно.
3. **Токсичность в мире** и **жижа из дезоляции** — см. §3.6–3.7.
4. **Молоко и `/effect clear`**: некроз, тремор и приступ защищены (`UncurableEffects`), остальное
   снимается как обычно.

---

## 8. Открытые вопросы к автору (сводно)

Актуальные — §3 (14 пунктов). Закрытые в этой фазе (для истории): цвет подписей-слов (оставить
цветными), хазмат-тултип (подключён), формат единиц (пробел везде), дозиметр (текст «Дозиметр:
Уровень облучения поднят до …»), подсказка доски (блок остаётся доступным навсегда, первым в
лотке — только на время той доски), флаг суневента (всем онлайн — задумка), таймеры брожения
(чистятся на выходе — норм), `solar_watch` (составной гейт «Открытие 2 И багровый день»).

---

## 9. Закрыто за эту фазу (22.09.2026)

Чтобы не искать по 1800 строкам аудита:

| блок | что закрыто | где |
|---|---|---|
| Аудит №1–№6 | локализация, шкалы, препараты-спека, психика-3, эффекты, нерфы, фейк-смерть | аудит §9.1–9.29 |
| №7 ГОСТ единиц | цвета GTU/GTH/воды/пара/предметов везде, радиация `#ceeb2d`, токсичность `#d12176`, пробел перед единицей, слово-единица у предметной трубы | `UNITS-GOST-2026-09-22.md`, аудит §9.31 |
| №8 Подсказка доски | `/gonzotech debug clue`, подсветка блока + выдача, партиклы, стресс за botched (+1500), одноразовые подсказки в чат | аудит §9.32 |
| Уточнения автора | хотбар — только для кровати; дозиметр говорит о дозе; «Сердце прихватило…»; регистр Гонзо | аудит §9.33 |
| Регистр и лор | «Меня/Я/Мне» с большой буквы, лор хазмата у 4 частей, вкладка «Познание» | аудит §9.34 |
| Баг статов | гейт статов слитков: «пока носишь» → «навсегда после Открытия 2» + ревизия всех гейтов | аудит §9.35 |
| Солнечные часы | гейт «И» (Открытие 2 + багровый день) на крафте и в книге, новая стр. 36 заметок | аудит §9.36 |
| Итог фазы | сводка и закрытие фазы | аудит §9.37 |
