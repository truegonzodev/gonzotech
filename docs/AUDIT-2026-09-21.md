# Gonzo Tech — полный технический аудит

**Дата:** 2026-09-21 (вечернее обновление)
**Ревизия:** `cf29546` («src gen»), ветка `arena/01a0c4a5-gonzotech`. За день прошли:
`c2a72a8` (первый заход чистки, 62 файла), `10124e1` («g wrapper»), `cf29546` («src gen»).
**Версия мода:** `0.0.2`, NeoForge 21.4.157, MC 1.21.4, Java 21, Parchment `2025.02.16`
**Тип проверки:** статический (чтение кода/ресурсов + скриптовые перекрёстные ссылки).
Gradle/игра **не запускались**: в песочнице нет Java (`./gradlew --version` → «JAVA_HOME is not
set»), поэтому всё дальше — выводы из исходников, а не из рантайма.

---

## 0. Как читать этот документ

| Раздел | О чём |
|---|---|
| 1 | Репозиторий: структура, метрики, инфраструктура |
| 2 | Ревизия документации (что актуально, что устарело) |
| 3 | Карта реализованного (что реально есть в коде) |
| 4 | **Ответ на главный вопрос:** неиспользуемое, без функционала, заглушки |
| 5 | Ассеты, локализация, битые ссылки |
| 6 | Мусор и риски репозитория |
| 7 | Куда двигаться дальше (P0/P1/P2 + бэклог) |
| 8 | Приложение: что добавлено первым заходом (радиационный экран, цирконий) |
| 9 | Разбор пунктов с автором: решения, статусы, датаген, заделы |

Ссылки вида `Файл.java:123` — точные строки в текущей ревизии.

---

## 1. Репозиторий: структура и метрики

```
gonzotech/
├── build.gradle, settings.gradle, gradle.properties   (moddev 2.0.144)
├── gradlew, gradlew.bat, gradle/wrapper/*             (wrapper 9.7.1 — добавлен 21.09)
├── README.md            — «живой» статус проекта
├── TEMP-notes.md        — инженерные заметки + бэклог (180 строк)
├── GIT-WORKFLOW.md      — 5 строк, не workflow
├── docs/                — 8 документов (см. раздел 2)
└── src/main/{java,resources}
```

### 1.1. Код

| Пакет | Файлов | Строк | Что внутри |
|---|---:|---:|---|
| `machines` | 176 | 20 788 | машины, BE, меню, экраны, сети/трубы, сплавы, CTM |
| `chalkboard` | 42 | 8 860 | доска резонанса, движок формул, заметки учёного |
| `core` | 41 | 5 154 | реестры, руды, psyche, события, миражи/мировоззрение |
| `space` | 21 | 4 773 | измерения, скайбоксы, ЧД-шейдер, команды |
| `radiation` | 10 | 1 381 | дозиметрия, распространение, containment |
| `mixin` | 19 | 1 119 | 19 зарегистрированных (мёртвый удалён 21.09) |
| `swag` | 8 | 672 | миска питомцев, «жирность» котов/волков |
| `sunevent` | 5 | 577 | багровые дни, снег, стабильность солнца |
| `GonzoTechMod.java` | 1 | 177 | корневой `@Mod`-класс |
| `mind` | 1 | 4 | **только `package-info.java`** |
| **Итого** | **324** | **43 505** | |

> Числа — на `cf29546`, после удаления `LevelRendererSunMixin` и правок комментариев.

### 1.2. Ресурсы

| Артефакт | Кол-во |
|---|---:|
| JSON всего | 2 446 (после чистки; все валидны) |
| PNG | 582 (+19 `.mcmeta`) |
| `assets/gonzotech/blockstates` | 199 |
| `assets/gonzotech/models/block` | 219 |
| `assets/gonzotech/models/item` | 402 (из них 150 — обёртки на block-модели, см. 6.1 п.9) |
| `assets/gonzotech/items` (дефиниции 1.21.4) | 444 |
| `data/gonzotech/recipe` | 577 |
| `data/gonzotech/loot_table` | 198 (197 блоков + 1 прочее) |
| `data/gonzotech/advancement` | 232 = 16 «своих» + 216 в `recipes/` |
| `data/gonzotech/worldgen` | 113 |
| таги (`data/gonzotech/tags` + `data/minecraft/tags`) | 34 |
| `lang/en_us.json` = `lang/ru_ru.json` | 644 = 644 ключа |

Типы 577 рецептов: `crafting_shaped` 174, `smelting` 116, `crafting_shapeless` 115, `blasting` 115,
`smoking` 45, `stonecutting` 6, кастомные `gonzotech:alloy_{pickaxe,sword,chestplate,helmet,leggings,boots}` — 6.

### 1.3. Инфраструктура — что отсутствует

* **Нет Gradle Wrapper** (`gradlew`, `gradlew.bat`, `gradle/wrapper/*` не закоммичены). Свежий клон
  не соберётся без локально установленного Gradle нужной версии; при этом внутренняя документация
  (`SelfTest.java`) прямо предлагает `./gradlew compileJava` — команда в репозитории не работает.
* **Нет CI** (`.github/` отсутствует), нет тестов (`src/test` нет), нет datagen
  (`src/generated` нет, хотя `build.gradle` его подключает как resource-src).
* **История git — 1 коммит.** Диффы по фичам недоступны, «кто/когда сломал» восстановить нельзя.
* `.gitignore` игнорирует `info/` — а именно на `info/gonzo_tech_chalkboard_design.md` ссылается
  комментарий в `ModBlocks.java:56`. То есть часть проектной документации живёт вне репозитория.

---

## 2. Документация: ревизия

### 2.1. Инвентарь

| Файл | Строк | Заявленный статус | Фактическое состояние |
|---|---:|---|---|
| `README.md` | 140 | живой статус | Актуален по духу, **несколько фактических ошибок** (см. 2.2) |
| `TEMP-notes.md` | 180 | рабочие заметки + бэклог | Актуален: §0 CTM-турбина, §1 ЧД, §2.1–2.7 бэклог, §3 направления |
| `docs/TEMP_NOTES.md` | 349 | внутренняя шпаргалка | **Самая ценная техническая документация** (GUI-стандарт, тир-2, тинт, суневеты, деслик). Хаотична: это «простыня» без оглавления |
| `docs/LOGISTICS-TEMP-NOTES.md` | 367 | «пласт закрыт 2026-09-08» | Точно описывает модель труб/пакетов/фильтров. Содержит невыполненные пункты плана (крышка в пучке, мультипоток узла) и список незакрытых текстур |
| `docs/MATERIAL_PROCESSING_CONCEPT.md` | 555 | концепт переработки | Реализована только ЦФ1УР; ЦФ2УР/ЦФ3УР — очередь (заявлено и там) |
| `docs/ALLOY_SYSTEM_DESIGN.md` | 191 | дизайн сплавов | Актуален: каталог, векторы статов, формулы |
| `docs/ALLOY_TINT_PALETTE.md` | 66 | палитра тинтов | Вспомогательный, актуален |
| `docs/CHALKBOARD-SERVER-AUTHORITY-PATCH.md` | 449 | закрыт | Исторический, полезен как описание валидатора формул |
| `docs/CONTENT_AUDIT_2026-09-14.md` | 54 | предыдущий аудит | **6 открытых продуктовых вопросов не закрыты** (см. 2.3) |

### 2.2. Расхождения «доки ↔ код» (найдено статически)

1. **(пофикшено 21.09)** **README: «книга заметок учёного (14 страниц, фоны по эре)».** В коде —
   `ScholarNotesContent.java` создаёт **40 страниц** (31 в главе ERA_1 + 9 в ERA_2), а в
   `lang/*.json` переведены только **p1…p14**. Страницы 15–40 (26 страниц, 46 ключей)
   выводят сырые ключи (см. 5.1). README исправлен: «40 страниц по 5 главам-эрам
   (вкладки + иллюстрации; наполнены I–II)». Дыра с ключами p15…p40 в lang при этом **остаётся**
   (это не ошибка документации, а незакрытая работа по локализации).
2. **(пофикшено 21.09)** **README: «26 руд, генерация с биомными бонусами/штрафами».** Биомных бонусов/штрафов в коде нет:
   руды добавляются `neoforge:add_features` в `#minecraft:is_overworld` без биомных модификаторов
   (`data/gonzotech/neoforge/biome_modifier/add_ores.json`). Есть бонусы **по вмещающей породе**
   (дробилка/центрифуга), это другое. README исправлен: «генерация жилами (stone/deepslate/nether)
   + замещение кальцита геод; бонусы по вмещающей породе (дробилка/центрифуга), а не по биомам».
3. **`OreDefinition.java:57`**: комментарий «автоворлдгена для CALCITE пока нет — нужен отдельный
   Feature» **устарел**: `MineralReplacementFeature` уже заменяет кальцит геод на
   `calcite_calcium_ore` с шансом 10 % (документировано в самом классе).
4. **`machines/package-info.java`**: «провода появятся позже» — провода/трубы реализованы
   полностью (5 типов + универсальная + узлы + пучки).
5. **`ModItems.java:135`**: «Пока это только зарегистрированные ингредиенты с placeholder-ресурсами:
   рецепты и машинная переработка будут добавлены отдельной задачей» — грит-цепочка
   (`trio_grit`, `clinker_grit`, `armor_mix`, `andesite_silicate_clinker`, `white_porcelain_batch`,
   `rebar`) уже имеет рецепты. Комментарий устарел.
6. **`PlayerPsyche.java:10–14`**: «облучение — задел», хотя `RadiationSystem` уже пишет туда дозу
   (`RadiationSystem.java:173,233`).
7. **README: «Психика… задел»** — сейчас в psyche реально живёт только радиационная шкала, остальные
   пять шкал пишутся/читаются, но **никогда не изменяются** (см. 4.6).
8. **`docs/LOGISTICS-TEMP-NOTES.md`**: «Осталось (не код): финальные текстуры логистики + 5 фонов эр +
   page_11..14.png» — файлы `page_11..14.png` уже есть, но сама схема `page_N.png` **отменена**
   (см. `NoteIllustrationKind:5`), т.е. они теперь мусор.
9. **README: «Завод Сплавов 5×5»** — реализован не полностью: машина есть, но приём 25 слотов и
   выход — да, а вот позиционирование «25 слитков на выходе» из `TEMP-notes.md §2.4` в коде
   не подтверждается (один предмет `custom_alloy` за операцию); это скорее разница формулировок.

### 2.3. Открытые вопросы прошлого аудита (`CONTENT_AUDIT_2026-09-14.md`), всё ещё актуальные

1. Electric Furnace II: 220 GTU/предмет против 200 у I — скорость выше, энергоэффективность **хуже**.
   Нужно решение: это overclock или баг баланса.
2. Cobble Generator II: по сравнению с I + алмазная/незеритовая кирка — **медленнее и дороже**
   (60 тиков/1.4 GTU против 50 тиков/0.8 GTU). Нужен baseline.
3. Pump II: у I есть предметные слоты (ведро/флакон), у II — нет. Специализация или регрессия?
4. Reject-механика Scavenger I/II: нужны 4 игровых сценария проверки.
5. Визуальная проверка 8 новых GUI-листов (слоты/маска/z-order).
6. Мультиплеер-проверка HUD ключа (payload, 5 тиков, пустая очередь).
7. **Новое (не было в прошлом аудите):** обязательная проверка радиации на выделенном сервере
   (см. 3.8) и модели предметов 1.21.4 (см. 5.2).

---

## 3. Карта реализованного

### 3.1. Прогрессия и доска

* 16 «Открытий» (`discovery_1..16`) — предметы-свидетели + advancements
  (`data/gonzotech/advancement/*`) и 216 reward-advancement'ов на рецепты.
* `RecipeUnlocks`: показ рецептов в книге по тирам 1, 2, 6 + «всегда» (3 шт.) + «через 20 минут»
  (3 шт.). Тиры 3, 4, 5 **не используются** — открытий 3/4/5 в книге рецептов нет.
* Гейты крафта: `Phase3Events` (Открытие 1) и `TierTwoCrafting` (Открытие 2) — до открытия крафт
  выдаёт `botched_mechanism` и тратит ингредиенты (35 id в `TierTwoCrafting.RECIPE_IDS` — все JSON
  на месте).
* Движок формул: `chalkboard/core` (Expr/Analysis/Evaluator/GameSolver/Manipulate), headless-тест
  `SelfTest.java` (не вызывается из игры и не подключён к сборке).

### 3.2. Геология и руды

* 26 руд, host-вариантов 46: STONE/DEEPSLATE/NETHER + специальный CALCITE (10 % замена кальцита геод
  через `MineralReplacementFeature`).
* Пыль/самородки/блоки: 47 слитков (`Metals.INGOT_IDS`), правила `NO_DUST={iodine,sulfur,mercury}`,
  `NO_NUGGET={iodine,manganese,sulfur}`, `SOFT_STORAGE={lead,lithium}`.
* 26 raw-предметов, у всех есть smelting/blasting-цепочка (проверено скриптом).
* Компоненты переработки (грит-цепочка) — рецепты присутствуют.

### 3.3. Паровая ветка

Топка → котёл → Стирлинг → электропечь → конденсатор → помпа → аккумулятор → генератор булыжника;
прямоугольная паровая турбина (Smart CTM, сервер хранит только `formed`) и парогенератор 5×5×5
(ядра + теплообменники с множителем `M = 1 + E_avg·(1 + 0.1·(n−1))`).

### 3.4. Тир-2 / атомный пласт

Дробилка, ЦФ1УР, измельчитель II, пресс II, электропечь II (2 линии), помпа II, аккумулятор II,
генератор булыжника II, завод сплавов II (5×5), ядерная топка, полный набор труб/узлов II,
фильтр/отсеиватель II. Гейт — `TierTwoCrafting` + книга рецептов.

### 3.5. Сплавный завод и сплав

* 14 именных пресетов (`AlloyFoundryRecipes`): cast_iron(1), steel(3), stainless_steel(7),
  corten_steel(25), nitinol(2), invar(6), ferromagnetic(5), cantor(5), vr20(5), stellite(11),
  alnico(10), telluride(5), vitreloy(9), semiconductor(4) — в скобках минимальный размер партии
  в «порциях» (10 единиц = 1 слиток/пыль; 10 самородков = 1 порция).
* Generic-сплав требует ≥ 2 разных материалов и ≥ 2 порций; каталог `AlloyMaterialCatalog` = 52
  материала (47 Gonzo + 5 ванильных), recipe-only уголь/глина не образуют generic-массу.
* Предмет `custom_alloy` несёт состав в data component; тинт — `AlloyTintSource`/`AlloyClient`;
  броня/пика/меч — через 6 кастомных типов рецептов + `AlloyEquipmentRecipe`.
* Статы тултипов материалов — `MaterialStatTooltips` (активен, `@EventBusSubscriber`, работает от
  «Открытия 3»).

### 3.6. Логистика

5 типов труб (WIRE/HEAT/WATER/STEAM/ITEM) + универсальная жидкостная + узлы + универсальный узел
(−10 % throughput) + пучки с послойным поворотом ключом. Модель пассивная: трубы не хранят и не
тикают, BFS-слив за тик, равномерная раздача (`Transfer.distributeAmong`).
Throughput: heat 388 GTH/t, wire 38 GTU/t, вода/пар 1000 mB/t, универсальная 800 mB/t суммарно,
предметы 5/тик суммарно (1/тик на вид).

### 3.7. Радиация (новая подсистема, документации почти нет)

* `RadSources` (mZt/с на слиток): uranium 0.02, thorium 0.007, plutonium 3.6, radium 75;
  формы: самородок ÷9, пыль ×2, блок ×9. Прямые эмиссии: U-238 0.01, U-235 0.10, U-233 1.50,
  weapons Pu 1.20, Pu-238 12.0, Pu-242 0.30, Th-229 0.80. Топлива: U 0.03, MOX 0.45, TMOX 0.35,
  SNUP 0.85.
* `RadMaterials` (доля прохождения): tungsten 0.003, vr20 0.01, lead/barium 0.02,
  osmium/iridium 0.05, boron 0.13, Li/Mg/Zn 0.2, Fe/Cu/Sn/Ni/Co/Cr/Mn 0.5, прочие металлы 0.3,
  неметаллы 1.0.
* `Containment`: BFS полости от источника, `VISIT_CAP=2048`, `RANGE=32`, `CACHE_TTL=400` тиков,
  `CACHE_MAX=4096`; фактор = среднее по материалам стенок; руки игрока не экранируют.
* `RadiationSystem` пишет дозу в `PlayerPsyche.radiation`; `PsycheHud` показывает её только
  с дозиметром в руке; `RadTooltip` показывает эмиссию предметов.
* **Документации по подсистеме нет вообще** — ни в README, ни в `docs/`, ни в `TEMP-notes.md`.

### 3.8. Космос

8 измерений + 8 dimension_type (moon, mars, europa, solar_orbit, alpha_centauri_orbit, deep_space,
blackhole_yx989_k2, blackhole_zangler_11), кастомный sky-renderer на каждое измерение **и на
Оверворлд** (`SpaceClient:184`), релятивистский шейдер ЧД (GLSL, геодезические Верле), Zero-G полёт
(0.007F), `SpaceCommand` (tp, sun/star-дебаг, alpha/y989/zangler/dyson, «Икар»-сброс).
Растительность/структуры Марса и Луны: `olympus`, `mars_pillars`, `moon_craters`, `mars/moon_rich_vein`,
`europa_ice`.
`data/minecraft/dimension/overworld.json` — **полная копия ванильного оверворлда на 1.5 МБ**, в которой
4 записи multi_noise заменены на `gonzotech:desolation`.

### 3.9. Суневеты (багровые дни)

`SunEventData` (SavedData): первый ивент — 10-й день, гэп сокращается до 4; множитель солнца
−1 %/день, пол 10 %; персистентный `SunState` (DEFAULT/DYSON/GONE/BLACKHOLE/BLACKHOLE_DYSON).
Клиент: покраснение неба, снег вместо дождя, монстры днём (`SunEventSnowRenderer`,
`SunEventClient`, `MobSunEventBurnMixin`, `MonsterSunEventSpawnMixin`).
Солнечная панель (`SolarPanelBlockEntity`) читает `solarMultiplier()` — **но крафта у неё нет** (см. 4.3).

### 3.10. Психика и сваг

`core/psyche` 375 строк: `PlayerPsyche` (6 шкал 0..1000 ‰), `PsycheHud`, `PsycheNetwork`,
`ModPsycheAttachments`. Реально живёт: `addiction` (+0.1 % за порцию браги) и `radiation`
(`RadiationSystem`). `stress`, `crisis`, `uv`, `chemical` — заделы: их сеттеры не вызываются ни разу.
`swag`: миска питомцев (+цель ИИ, жирность котов/волков через 5 миксинов) — 672 строки, полностью
рабочая шутка. `mind` — пустой пакет (4 строки `package-info`).

---

## 4. Неиспользуемое, без функционала, заглушки

> Это ответ на основной вопрос задания. Ниже — только проверенные факты, с указанием источника.

### 4.1. Слитки: **39 из 47** не имеют ни одного собственного применения

Скрипт: для каждого слитка собраны все рецепты, где он — ингредиент, кроме «своих» (блок/самородок/
обратный обмен). Результат:

* **Полноценно используются (8):** `cast_iron` (26 крафтов), `steel` (6), `calcium` (6),
  `ferromagnetic` (1 — `inductive_module`), `nickel` (1 — `solar_watch`), `aluminum` (2 — провода),
  `lead` (2 — стекло + ядерная топка), `tungsten` (2 — топка + завод сплавов).
* **39 слитков** имеют только «самореферентные» рецепты (слиток↔блок↔самородок↔пыль):
  magnesium, sulfur, manganese, titanium, barium, zinc, tin, boron, chromium, cobalt, silver, iodine,
  mercury, uranium, zirconium, thorium, platinum, tellurium, palladium, cesium, iridium, osmium,
  stainless_steel, corten_steel, plutonium, nitinol, invar, neodymium, cantor, vitreloy, semiconductor,
  vr20, stellite, alnico, telluride, bismuth, rhenium, radium, lithium.

**Важная оговорка (не преувеличивать проблему):** эти 39 металлов *не выброшены из игры* — они
принимаются Заводом Сплавов II (`AlloyMaterialCatalog`, 52 материала: 47 Gonzo + 5 ванильных), а их
пыль переплавляется в слитки. То есть у них есть «сток экономики», но **нет ни одного собственного
рецепта-цепочки, ни одного применения в машинах/инструментах**. Практически: игрок добывает
редчайший `osmium`/`iridium`/`rhenium` и может только засунуть его в сплав или в блок.

**Отдельно про «странные» металлы:** `sulfur`, `iodine`, `mercury` не имеют ни пыли, ни самородка
(по правилам `Metals`) и не участвуют ни в одном рецепте, кроме своих же блоков. Это **кандидаты №1
на функционал** (кислоты, яды, хим. обработка — ср. `docs/MATERIAL_PROCESSING_CONCEPT.md`).

### 4.2. Пластины и ядра: **9 предметов-тупиков**

Пресс II производит 10 пластин и 2 ядра; из них **9 не используются вообще нигде**:

| Предмет | Производится | Потребляется |
|---|---|---|
| `copper_plate`, `aluminum_plate`, `gold_plate`, `stainless_steel_plate`, `redstone_plate`, `titanium_plate`, `semiconductor_plate` | Пресс II (`PressRecipes.PLATES`) | **нигде** (0 рецептов, 0 ссылок в коде) |
| `redstone_core`, `semiconductor_core` | Пресс II (`PressRecipes.CORES`) | **нигде** |
| `iron_plate`, `steel_plate`, `nickel_plate` | Пресс II | используются (тир-2 машины, провода) |
| `copper_wire`, `aluminum_wire`, `gold_wire`, `silver_wire` | Пресс II | используются (`coil`, `inductive_module`, провода II) |

Это самая «обидная» категория: 7 ценных слитков (включая редкие титан/нержавейку/полупроводник)
тратятся на предметы, которые затем нельзя никуда применить. Либо им нужны применения
(обшивки, корпуса, кор-блоки машин), либо это осознанная заготовка под будущие тиры — в коде
об этом **нет ни одного комментария**.

### 4.3. Предметы и блоки без способа получения (только креатив)

Проверено перекрёстно: рецепт + лут + машинная переработка + выдача из кода.

| Объект | Рецепт | Лут | Комментарий |
|---|---|---|---|
| `dosimeter` | **нет** | нет | Ключевой предмет радиационной системы: без него игрок не видит дозу (`PsycheHud:79`). В коде помечен как рабочий инструмент, не как витрина |
| `uv_meter` | **нет** | нет | предмет и HUD-шкала рабочие, рецепта нет; шкалу `PlayerPsyche.uv` пока никто не наполняет — задел под УФ-механику (комментарий в `ModItems:255–257` приведён к этому виду 21.09) |
| `speedometer` | **нет** | нет | HUD-спидометр (`SpeedometerHud`) работает, получить нельзя |
| `solar_panel` | **нет** | есть лут-таблица | Блок, BE, HUD, «Открытие 4» — а крафта нет; **и нет локализации имени** |
| `solar_watch` | есть (`solar_watch.json`) | нет | …**но нет lang-ключей** (см. 5.1) |

### 4.4. Ядерный ряд: 11 предметов-витрин

`uranium_238/235/233`, `weapons_plutonium`, `plutonium_238/242`, `thorium_229`, `uranium_fuel`,
`mox_fuel`, `tmox_fuel`, `snup_fuel`:

* зарегистрированы (`ModItems.java:158–190`) и лежат во вкладке «Компоненты»;
* имеют радиоактивные пресеты в `RadSources.DIRECT_EMISSION`;
* **не имеют ни одного рецепта**, ни одного упоминания в `data/`, не принимаются ядерной топкой
  (топливо — только 8 «форм» урана/тория), не используются ни в одной машине;
* текстуры — **временные** (копии существующих слитков/пыли), это признано комментарием
  `ModItems.java:160`;
* **11 из них не имеют файла-дефиниции предмета** (`assets/gonzotech/items/*.json`) — см. 5.2.

### 4.5. Осознанные витрины (не считать багами)

* Вкладка «Приколы» (`ModCreativeTabs.GAGS_TAB`): `obsidian_bucket` (получается из лава-ведра через
  `BucketItemMixin`), `botched_mechanism`, `the_proto_mash`, `the_fruit_mash`,
  `singular_heat_source`, `singular_energy_source` — админ-предметы и шутки, часть без рецептов
  **по замыслу** (комментарий в `ModCreativeTabs:265`).
* `discovery_1..16` — предметы-свидетели прогресса, рецептов у них нет by design.
* `flat_punch`/`wedge_punch`/`ingot_form`/`plate_form`/`core_form` — селекторы режима Пресса II,
  а не ингредиенты (это документировано в README).
* `pseudo_coil` — стартовая «подделка» катушки, крафтится всегда.

### 4.6. Заглушки в коде (точные места)

| Файл:строка | Что это |
|---|---|
| `Phase3Events.java:199` | `// TODO(Фаза X): здесь начислять «стресс» игроку за преждевременный крафт` — единственный явный TODO игровой механики |
| `MachineDefs.java:361,364` | 2× `TODO: подтвердить баланс у автора` (макс. приём/отдача GTU) |
| `PlayerPsyche.java:10–14` | 4 шкалы описаны как «задел, пока 0»; `setStress/setCrisis/setUv/setChemical/setAddiction` **не вызываются ни разу** (проверено по всему проекту) |
| `ScholarNotesContent.java:40` | «Главы III–V пока пусты (заглушки-вкладки)»; из 5 глав наполнены 2 |
| `ItemFilterBlockEntity.java:46` | «Наибольшее число ghost-слотов у текущих уровней (нужно меню-заглушке)» |
| `ItemFilterMenu.java:152` | `/** Пустой контейнер-заглушка для позиционирования ghost-слотов. */` |
| `DesolationConversionFeature.java:91` | «временная диагностика: одна строка за сессию, потом уберём» + статический `Set<Long> doneChunks` |
| `space/package-info.java` | «Группа 2 (позже)», «Группа 3 (позже)» — орбиты/ЧД-миры описаны как будущее, хотя 8 измерений уже зарегистрированы |
| `machines/package-info.java` | ~~«провода появятся позже»~~ **(пофикшено 21.09)** — текст заменён на актуальный (логистика реализована) |
| `OreDefinition.java:57` | ~~устаревший комментарий про отсутствие ворлдгена CALCITE~~ **(пофикшено 21.09)** — теперь ссылается на `CALCITE_CHANCE` в `MineralReplacementFeature` |
| `ModItems.java:135` | ~~устаревший комментарий про «placeholder-ингредиенты без рецептов»~~ **(пофикшено 21.09)** |
| `ModBlocks.java:56` | ссылка на `info/gonzo_tech_chalkboard_design.md` — папка `info/` в `.gitignore` и в репозитории отсутствует **(пофикшено 21.09)**: текст переписан на «док автора вне репозитория», чтобы ссылка не выглядела битой |
| остальные комментарии | **(пофикшено 21.09, второй заход)** ещё 11 мест в 8 файлах — полный список в 6.3 |
| `mind/package-info.java` | весь пакет = 4 строки описания «экзистенциальный стресс, психоз, глитчи» и ни одной реализации |
| `SelfTest.java` | ~~headless-тест движка формул: не подключён к Gradle~~ **(пофикшено 21.09)** — заведён Gradle-таск `./gradlew selfTest` (см. 9.2) |

### 4.7. Мёртвый код

| Объект | Доказательство |
|---|---|
| ~~`mixin/client/LevelRendererSunMixin.java`~~ **(удалён 21.09)** | был **не зарегистрирован** в `gonzotech.mixins.json` (19 из 20). Единственный потребитель `SpaceSkyState.getOverworldSunTexture()` — тоже никто: подмена солнца в Оверворлде делается кастомным sky-эффектом (`SpaceClient:184`, `renderSky` возвращает `true` и рисует солнце сам через `resolveVariant`). Удалены и миксин, и метод |
| ~~`chalkboard/core/SelfTest.java`~~ **(пофикшено 21.09)** | см. выше — перестал быть мёртвым: запускается таском `selfTest` |
| ~~9 legacy `models/item/<ore>_ore.json`~~ **(удалены 21.09)** | `chromium_ore, cobalt_ore, iridium_ore, osmium_ore, palladium_ore, platinum_ore, tellurium_ore, tungsten_ore, uranium_ore` — ссылаются на block-модели deepslate-вариантов; в 1.21.4 не используются (дефиниции живут в `items/`) |
| ~~2 legacy текстуры брони~~ **(удалены 21.09)** | `textures/models/armor/custom_alloy_layer_1/2.png` заменены на `textures/entity/equipment/humanoid{,_leggings}/custom_alloy.png` |
| ~~14 текстур страниц~~ **(удалены 21.09)** | `textures/gui/notes/page_1..14.png` — старая схема отменена (`NoteIllustrationKind:5`), нынешняя использует `page_crafting_*` |
| 2 orphan-текстуры | `block/third/silicon_factory`, `gui/reference_512` (референс-лист для художника, кодом не читается). **Оставлены:** автор подтвердил, что это заготовки «наперёд» (как было с barium/bore-замесами) |
| ~~6 заготовок~~ → **введены 21.09.2026** | `block/industry/barium_concrete`, `block/industry/bore_stained_glass`, `item/barium_mix`, `item/bore_silicate_mix`, `item/components/UT_fuel`, `item/components/zirconium_plate` — текстуры были нарисованы заранее под радиационный экран и циркониевую пластину, теперь они в игре (см. раздел 8) |
| ~~`gui/notes/notes_bg.png`~~ **(удалён 21.09)** | фон без эры, заменён `notes_bg_era1..5` |

### 4.8. Прочее, что стоит знать

* Статический `Set<Long> doneChunks` в `DesolationConversionFeature` — **никогда не чистится** и не
  сбрасывается по границам измерения: медленная утечка и потенциальное «не сработает после
  перезахода» (требует проверки в игре).
* **(пофикшено 21.09)** `data/gonzotech/advancements/` — **дубликат-папка**: 16 тех же файлов, что и в
  `data/gonzotech/advancement/`, но без `recipes/`. В 1.21.4 живая — `advancement/` (единственная,
  на которую ссылается лут/код), `advancements/` — мёртвая копия (16 JSON = ~10 КБ мусора и
  классическая ловушка «правлю не тот файл»).
* **(пофикшено 21.09)** `hollow_grid_sphere_rotation_32frames.png` (78 КБ) в корне репозитория —
  арт-референс сферы Дайсона, нигде не использовался; файл удалён из репозитория (храни локально/в `docs/`).

---

## 5. Ассеты, локализация, ссылки

### 5.1. Локализация: **63 реальных отсутствующих ключа** (+ 14 объектов без имён)

Проверено: `en_us.json` и `ru_ru.json` идентичны по составу (644 = 644), «мёртвых» ключей нет,
но есть **отсутствующие**. Код ссылается на 204 `*.gonzotech.*` ключа; 67 из них не найдены в lang,
из которых 4 — артефакты динамических префиксов (`item.gonzotech.discovery_`,
`message.gonzotech.pipe_mode_short.` и т. п.), т.е. **реально отсутствуют 63 ключа**:

| Группа | Кол-во | Последствие |
|---|---:|---|
| `gui.gonzotech.notes.p15…p40.{title,body}` — **страницы 15–40 (26 из 40)** | 46 | **Вся вторая половина «Заметок учёного» показывает сырые ключи** в обеих локалях |
| `gui.gonzotech.steamgen.*` (gth_in, water_in, steam_out, cores, exchangers, rated, steam_made) | 7 | Тултипы парогенератора 5×5 — сырые ключи |
| `gui.gonzotech.notes.illustration.*` (crafting, foundry, structure, view_side, view_top) | 5 | Подписи-иллюстрации заметок |
| `gui.gonzotech.nuclear_firebox.burning`, `gui.gonzotech.smelting_progress`, `gui.gonzotech.second_cobble_generator.generation_progress` | 3 | Тултипы прогресса |
| `hud.gonzotech.solar_watch` | 1 | HUD солнечных часов |
| `block.gonzotech.steamgen_core` | 1 | Имя блока ядра парогенератора (у `steamgen_casing` ключа нет вообще — см. ниже) |

Плюс **14 зарегистрированных объектов вообще без lang-ключей** (не только GUI; пересчитано 21.09 —
совпадает: 13 из них имеют `items/*.json`, 14-й — `molten_corium`, у него ключ есть, но флюидный):
`alloy_helmet`, `alloy_leggings`, `alloy_boots` (крафтятся, носят имя-ключ),
`charred_sapling`, `scorched_tuft(+_large/_medium)`, `dead_slime_bucket`, `pet_bowl`, `solar_panel`,
`solar_watch`, `steamgen_casing`, `steamgen_core`, `molten_corium`.

> Это самая массовая и самая «дешёвая» для исправления проблема: тексты страниц заметок, судя по
> всему, уже написаны, но лежат вне репозитория (или не были добавлены в lang).

### 5.2. Предметные модели (формат 1.21.4)

В 1.21.4 рендер предмета задаёт **`assets/<ns>/items/<id>.json`** (а не `models/item/`).
Первоначально дефиниции не было у 14 id; после разбора с автором картина такая:

* **(пофикшено 21.09)** **11 ядерных предметов** (`uranium_238/235/233`, `weapons_plutonium`,
  `plutonium_238/242`, `thorium_229`, `uranium_fuel`, `mox_fuel`, `tmox_fuel`, `snup_fuel`) —
  у них уже были `models/item/*.json` и текстуры, не хватало только дефиниции (в игре это был бы
  «missing model»). Одиннадцать файлов по одной строке добавлены.
* **Сверка «блок ↔ предмет» (21.09), точные числа:** 199 блокстейтов, 191 items-дефиниция
  ссылается на `gonzotech:block/...` напрямую (канон 1.21.4), **8 блокстейтов без block-модели в
  `items/`** — но 5 из них (`charred_sapling`, `pet_bowl`, `scorched_tuft` + `_large`/`_medium`)
  оформлены плоскими item-спрайтами (`gonzotech:item/...`) и в игре рендерятся нормально.
  Остаются ровно три блока без предмета — те, что ниже.
* **(не дефект, снято 21.09)** `composite_pipe`, `second_composite_pipe`, `molten_corium` —
  у этих блоков **вообще нет предмета** (проверено: 107 блоков, 104 предмета; без предметов ровно
  эти три). Пучки формируются из труб, СКМ их не берёт (pick-block не включён); `molten_corium` —
  жидкостный блок (`LiquidBlock` + `ModFluids`), у него есть ведро `corium_bucket`, а лут-таблицы
  флюидным блокам не положены (у ванильной лавы её тоже нет). Им дефиниции не нужны.

### 5.3. Прочее по ассетам

* Все 46 ore-блоков **имеют** корректные `items/*.json` (подозрение «28 руд без item-моделей»
  **не подтвердилось** — это была ошибочная проверка по старому пути `models/item`).
* Все прямые ссылки `gonzotech:block/...` из моделей и все datapack-ссылки
  (`configured_feature`, `placed_feature`, `dimension`, `biome`, `carver`, `noise_settings`,
  `biome_modifier`) — **разрешаются**, битых нет.
* 52 GUI-PNG на месте, все пути из `MachineClient`/`ItemFilterScreen` существуют.
* Текстуры окружения: `environment/overworld/{sun,sun_red,sun_dyson,sun_red_dyson,sun_gone,
  sun_blackhole*,moon_phases}.png`, `dyson_ring.png` — используются SpaceSkyEffects.

---

## 6. Мусор и риски репозитория

### 6.1. Мусор — **вычищено 21.09**

| # | Объект | Статус |
|---|---|---|
| 1 | `data/gonzotech/advancements/` — дубликат-папка (16 файлов) | **удалено** |
| 2 | 9 legacy `models/item/<ore>_ore.json` | **удалено** |
| 3 | 14 `textures/gui/notes/page_1..14.png` + `notes_bg.png` | **удалено** |
| 4 | 2 `textures/models/armor/custom_alloy_layer_*.png` | **удалено** (пустая папка `textures/models` вычищена) |
| 5 | `mixin/client/LevelRendererSunMixin.java` | **удалено** (вместе с мёртвым методом `getOverworldSunTexture`) |
| 6 | `hollow_grid_sphere_rotation_32frames.png` из корня | **удалено** |
| 7 | 2 orphan-текстуры (`silicon_factory`, `reference_512`) | **оставлено** — заготовки «наперёд» (подтверждено автором) |
| 8 | `GIT-WORKFLOW.md` | оставлено — авторский рабочий файл, правится вручную |
| 9 | 150 `models/item/*.json`-обёрток на block-модели | **не удалено, готово к удалению**: в 1.21.4 такие обёртки не читаются (149 из 150 нигде не упомянуты; единственное исключение — `pet_bowl`, чья `items/`-дефиниция ссылается на обёртку). Удалять одним пакетом после первого запуска игры, чтобы убедиться в отсутствии новых «missing model» |

После чистки: 2 446 JSON (все валидны), 0 битых ссылок моделей/рецептов/тегов,
у каждого блока есть лут (кроме флюидного `molten_corium` — так и должно быть).

### 6.2. Риски

| Риск | Почему |
|---|---|
| **Переопределение ванильного мира** | `data/minecraft/dimension/overworld.json` (1.5 МБ) — полная копия ванильного оверворлда. При апдейте MC это надо синхронизировать вручную; другие моды, добавляющие биомы в оверворлд через датапак-механику, могут конфликтовать с этой копией |
| **Полная подмена sky-эффекта Оверворлда** | `SpaceClient:184` регистрирует свой `SpaceSkyEffects` на `minecraft:overworld` — конфликты с любыми мод-скайбоксами/шейдерами (Iris/Oculus) практически гарантированы; документации об этом нет |
| **`doneChunks` не чистится** | `DesolationConversionFeature` — утечка/корректность на длинных сессиях |
| **Нет wrapper/CI** | нельзя воспроизводимо собрать и проверить PR-ы. Рекомендация: добавить wrapper (см. 9.1) — на своей машине он не нужен, нужен «другим»: CI, контрибьюторам и любой автоматизации |
| **Нет датагена** | 577 рецептов, 198 лут-таблиц, 199 блокстейтов — всё руками; любая ошибка ловится только игрой. Разбор и план — 9.3 |
| **Дублирование источника правды** | одни и те же числа живут в `README`, `TEMP-notes.md`, `docs/TEMP_NOTES.md`, `docs/CONTENT_AUDIT_*` и в коде; уже сейчас 9 расхождений (2.2) |
| **Один коммит истории** | нет возможности найти регрессию диффом, вся история — в ветках PR |


### 6.3. Комментарии: второй заход (21.09, вечер)

Задача автора: «убрать дезинфу или мёртвые коммы», а заглушки и пометки «на будущее» — оставить,
явно перечислив в аудите места вызова (это — 6.4). Прогон `audit/comment_scan.py` (маркеры
«позже / пока / задел / TODO / временное / …») сначала дал 78 комментариев; после разбора и правок
осталось 72 — и почти все оставшиеся это нормальные обороты речи («пока горит топливо», «пока
игрок держит ЛКМ»), а не расхождения с кодом.

Правки — **12 мест в 9 файлах + 2 строки README** (каждая проверялась grep'ом по фактическому коду;
компиляции не было — в песочнице нет Java):

| Файл | Что было не так | Что стало |
|---|---|---|
| `ModItems` (ScholarNotes) | не было числа страниц | 40 страниц в 5 главах-эрах, I–II наполнены, III–V — пустые вкладки |
| `ModItems` (бетоны) | «заготовки без рецептов» | рецепты есть — «свои рецепты» (проверено по `recipe/`) |
| `ModItems` (UV_METER) | «пока плейсхолдер без рецепта» | прибор рабочий, рецепта нет, `PlayerPsyche.uv` — задел под УФ-механику |
| `ModBlocks` (javadoc доски, стр. 54–56) | ссылка на `info/gonzo_tech_chalkboard_design.md` (файла нет, `info/` в .gitignore) | «дизайн-док автора вне репозитория» |
| `MachineDefs` + `PumpBlockEntity` | «слива в сеть пока нет» | слив работает через `network.PipeRouting` |
| `Sinks` | «жидкостных труб пока нет» | push работает сейчас, трубы — дополнение к нему |
| `PlayerPsyche` | «облучение — задел» (устарело) | явный список: ЖИВЫЕ — зависимость (`Phase3Events:458`) и облучение (`RadiationSystem:173,233`); остальные четыре шкалы — ЗАДЕЛ (пока 0) |
| `OreDefinition` (×2) | ссылки на «addendum» и «ворлдгена CALCITE нет» | одна ссылка на `CALCITE_CHANCE` в `MineralReplacementFeature` |
| `SunEventData` | не сказано, кто читает | `SolarPanelBlockEntity` читает `solarMultiplier()` |
| `SpaceSkyEffects` | «луна — repository placeholder» | луна — shipped-атлас `moon_phases`, рисуется здесь же |
| `README` (2 строки) | «14 страниц», «биомные бонусы/штрафы» | 40 страниц; бонусы — по вмещающей породе (host block) |

Проверено и **оставлено как есть** (это не дезинформация): TODO `Фаза X` про стресс
(`Phase3Events:199`), «подтвердить баланс у автора» (`MachineDefs:361,364`), приём пара
(`BoilerBlockEntity:121`), `StirlingBlockEntity:139` и ссылки на `TEMP_NOTES §3.5`.

### 6.4. Заделы «на будущее»: объявление → кто вызывает сегодня → точка врезки

Заглушки не удаляются. Ниже — их полная карта, чтобы «инфа везде совпадала». Проверка —
`audit/callers_scan.py` + `grep` по вызовам (номера строк на `cf29546`).

**Живые врезки (уже работают):**

| Механизм | Объявлен | Кто вызывает |
|---|---|---|
| `PlayerPsyche.setRadiation` | `core/psyche/PlayerPsyche.java:100` | `RadiationSystem:173` (набор дозы), `:233` (сброс) |
| `PlayerPsyche.addAddiction` | `PlayerPsyche.java:114` | `Phase3Events:458` (`MASH_ADDICTION_PER_EAT`, самогон) |
| шкалы psyche → HUD | `PsycheNetwork:31–36` | `PsycheHud:71–76` (клиент); сервер шлёт `NotesNetwork`-стиль payload'ом |
| `GtFormat.formatRate` | `machines/energy/GtFormat.java:78` | `WrenchHud:454` (живой поток труб) |
| `SunEventClient.crimsonIntensity/crimsonSunWindow` | `sunevent/client/SunEventClient.java` | `SpaceSkyEffects:341,416,431,788,807` |

**Заделы (0 внешних вызовов — осознанные точки роста):**

| Задел | Объявлен | Состояние сегодня | Куда планируется врезка |
|---|---|---|---|
| `PlayerPsyche.setStress / setCrisis / setUv / setChemical` (+`addStress/addCrisis/addUv/addChemical`) | `PlayerPsyche.java:92–116` | ни одного вызова; шкалы рисуются и синкаются, но всегда 0 | `Phase3Events:199` (TODO: стресс за преждевременный крафт), УФ — при вводе УФ-механики (`uv_meter` уже готов), химия — Открытие 3+ |
| `PlayerPsyche.setAddiction` | `PlayerPsyche.java:88` | 0 (живой путь — только `addAddiction`) | прямые сценарии «отравления» |
| `ItemRadioactivity.isLeadImmune` | `radiation/ItemRadioactivity.java:47` | 0 вызовов: иммунитета больше нет, метод оставлен как «свинец экранит ≥99 %» | проверка в тике `RadiationSystem`, когда экраны станут обязательными |
| `ItemRadioactivity.rawTagValue` | `ItemRadioactivity.java:131` | 0 | дебаг/совместимость с чужим `custom_data` |
| `GtFormat.packMantissa / packExp / unpackUnits / formatPacked` | `GtFormat.java:102,113,126,133` | наружу отдан только `formatRate` | сетевой синк больших буферов; комментарий-обещание уже стоит в `GtBuffer:105–113` |
| `ChunkRadiationData.contaminatedCount` | `radiation/ChunkRadiationData.java:341` | 0 (диагностика «сколько горячих чанков на учёте») | лог/дебаг-команда при отладке распространения |
| `SunEventClient.isEventDayNow` | `sunevent/client/SunEventClient.java:59` | 0 | клиентские визуалы «сегодня суневент» (сейчас берут `crimsonIntensity`/`crimsonSunWindow`) |
| `ScholarNoteFlags.SUN_FADE` | `chalkboard/notes/ScholarNoteFlags.java:21` | флаг ставится (`SpaceCommand:175`), читается (`ScholarUnlock:50`), страниц под GONE-сценарий пока нет | глава II заметок, если появится сюжет про угасшее Солнце |
| per-type оси пучка | `machines/network/CompositePipeBlock.java:52` | пучок работает без BE, с общим жидкостным углом | «Шаг 2» — per-type оси/multipart |
| «в будущем гелий» | `machines/network/PipeType.java:12` | 4 типа + универсальная | новые жидкости = новый тип |
| группы 2/3 космоса | `space/package-info.java:15,18` | 8 измерений уже есть | орбиты Юпитера/Солнца/Альфы Центавра, ЧД-миры, терминал, криокамера |
| топливные смеси под будущие реакторы | `machines/menu/NuclearFuelSlot.java:12` | смеси (MOX/ТСО/СНУП/УТ) зарегистрированы, реактора нет | продвинутые реакторы Открытия 3+ |

Вывод: «висящих» заделов, противоречащих текущему поведению, не осталось — все они либо прямо
описаны в javadoc как будущие, либо (шкалы psyche) помечены ЗАДЕЛ с точкой врезки.

---

## 7. Куда двигаться дальше

### P0 — сломанное/обещаное, но отсутствующее (1–2 дня работы, максимум пользы)

1. **Добить локализацию.** 67 ключей + 14 имён. Особенно `notes.p15…p40` — сейчас вся вторая
   половина книги заметок выглядит сломанной. Механика (40 страниц, иллюстрации, витрины)
   уже готова — нужны только строки в `en_us.json`/`ru_ru.json`.
2. **Рецепты для дозиметра / УФ-радиометра / спидометра.** Радиационная система без дозиметра
   неиграбельна по замыслу самого мода (`PsycheHud` показывает дозу только с ним).
3. **`items/*.json` для 11 ядерных предметов** (+ 3 блочных) — иначе «Открытие 2/атомный пласт»
   будет с пурпурными кубами. Одна строка JSON на предмет.
4. **Решить судьбу `solar_panel`**: дать крафт (он в «Открытии 4» и уже читает `solarMultiplier`)
   либо честно убрать из вкладки до соответствующей эпохи.
5. **Определиться с 9 dead-end предметами Пресса II** (см. 4.2): либо применения (обшивки/корпуса/
   апгрейды машин/ячейки), либо убрать из Пресса и вкладки. Сейчас это «ловушка для игрока».
6. **Удалить мусор из 6.1** — особенно дубликат `advancements/`.

### P1 — незавершённые подсистемы (то, что уже начато)

7. **Стресс/психика (TEMP-notes §2.1, `Phase3Events:199`)** — единственный явный TODO игровой
   механики: начисление стресса уже имеет точку врезки, шкалы и HUD готовы,
   не хватает источников и последствий. Это даёт смысл `botched_mechanism` и «Приколам».
8. **Решения по 6 открытым вопросам предыдущего аудита** (2.3) — они дешёвые по времени, но
   блокируют баланс: Electric Furnace II (overclock или эффективность), Cobble Gen II baseline,
   Pump II контейнеры.
9. **Комментарии-устаревшие (2.2) привести в порядок** — иначе следующий аудит снова будет
   искать несуществующие проблемы.
10. **Radiation: документация + тесты в игре.** Подсистема самая новая и самая недокументированная;
    в идеале — `docs/RADIATION.md` с таблицами `RadSources`/`RadMaterials`/`Containment`.
11. **`doneChunks`**: TTL или сброс по измерению.

### P2 — развитие (то, что даст игре «следующий слой»)

12. **Дать применение 39 «пустым» слиткам.** Три очевидных пути, по возрастанию объёма:
    * **инструменты/броня по тирам** (бор/титан/вольфрам: пилы, буры, скафандр) — прямо просят их;
    * **машины-потребители** (ядерная топка уже ждёт свинец/вольфрам — дать тот же приём
      осмию/иридию в абсорберах, радий/полоний — в источниках);
    * **химия**: `sulfur`/`iodine`/`mercury` — кислоты и реактивы, что закрывает и «мёртвые»
      неметаллы, и запрос пользователя.
13. **Структуры/руины.** Блоки `mechanisms`, `weathered_*`, `debris`, `silicon_cache`, `meteor`
    зарегистрированы и имеют лут-таблицы с ценными дропами (алюминий, свинец, кремний), но
    **worldgen-ссылок на них нет** (кроме метеоров) — это готовый контент, который игрок
    никогда не увидит. Нужны структуры/фичи (даже простые «кучи обломков»).
14. **Биомная специфика руд** (то, что README уже обещает): бонусы/штрафы по биомам — либо
    реализовать, либо убрать из README.
15. **Дальше по TEMP-notes §2.2–2.7:** скафандр/кислород/джетпак → литография/кремний → токамаки →
    Дайсон/губка Менгера. Задел уже есть: скайбокс Дайсона, `solarMultiplier`, `mission Икар`
    (`SpaceCommand` `reset()`), САМЕ измерение `deep_space`.

### 7.1. Что я НЕ проверял (честные ограничения)

* Компиляцию и запуск клиента/сервера (нет wrapper, нет сети).
* Рантайм-поведение радиации, содержания (containment), утечек `doneChunks`.
* GUI-выравнивание и маски (только существование PNG и путей).
* Дату-пак-валидность на живом сервере (только разрешение ссылок статическим скриптом).
* Совместимость с Iris/Oculus/шейдерами и другими модами.

---

### Резюме одной страницей

Мод **очень крупный и в целом в хорошем инженерном состоянии**: 43 тыс. строк, 572 рецепта,
197 блоков, 20 машин с GUI, полностью рабочие логистика/радиация/сплавы/космос, и на удивление
мало «настоящего» мусора — почти всё, что не используется, помечено комментариями как задел.

Главные проблемы — **не архитектура, а незакрытые концы**:

1. 39 из 47 слитков не имеют собственного применения (работает только завод сплавов);
2. 9 предметов Пресса II (7 пластин + 2 ядра) — чистые тупики, без комментариев;
3. 3 измерительных прибора и солнечная панель не крафтятся — при том, что система радиации
   без дозиметра неиграбельна;
4. 11 ядерных предметов-витрин без рецептов и без предметных моделей;
5. **26 страниц заметок учёного и 14 имён объектов без локализации** — самый заметный для игрока
   дефект;
6. Мёртвый миксин солнца, дубликат-папка `advancements/`, 9 legacy-моделей, 2 orphan-текстуры.

Первые три пункта P0 закрываются за один вечер и снимают почти весь «визуальный» дискомфорт
игрока; пункты P1 (стресс, баланс тир-2, документация радиации) — это следующая логичная
итерация; P2 (слитки→инструменты, руины, биомные бонусы) — уже полноценное развитие контента.

---

## 8. Приложение: добавлено 2026-09-21 (радиационный экран + циркониевая пластина)

Заготовки из раздела 4.7 введены в игру. Состав изменений:

**Новые предметы** (вкладка «Компоненты»):

| id | EN / RU | Получение |
|---|---|---|
| `barium_mix` | Shielding Mix / Экранирующий замес | бесформенный крафт: 3× бариевая пыль + 2× клинкерная крошка + 1× андезитовая крошка → **4 шт** |
| `bore_silicate_mix` | Borosilicate Mix / Боросиликатный замес | бесформенный крафт: 4× борная пыль + 2× (кремень **или** `gonzotech:silicon`) + 1× песок → **1 шт** |
| `zirconium_plate` | Zirconium Plate / Циркониевая пластина | Пресс II, режим «плоский пуансон + форма пластины»: слиток циркония → 1 пластина (как остальные 10 пластин) |
| `ut_fuel` | UT Fuel (Uranium-Thorium) / УТ-топливо (уран-ториевое) | пока компонент-заготовка (как MOX/ТСО/СНУП); эмиссия 0.20 mZt/шт |

**Новые блоки** (вкладка «Блоки»):

| id | EN / RU | Получение | Экранирование (предмет / стена контура) |
|---|---|---|---|
| `barium_concrete` | Barium Concrete / Бариевый бетон | 2×2 из 4× экранирующего замеса → 1 блок | **×0.03 / ×0.01** |
| `bore_stained_glass` | Boron Glass / Борное стекло | переплавка боросиликатного замеса в печи или доменной печи | **×0.08 / ×0.05** |

Прочее:

* `barium_concrete` — тяжёлый бетон (твёрдость 8.0, взрывоустойчивость 12.0, `requiresCorrectToolForDrops`),
  теги `minecraft:mineable/pickaxe` + `minecraft:needs_iron_tool`.
* `bore_stained_glass` — `TransparentBlock` как свинцовое стекло: ломается любым инструментом,
  дроп только с шёлковым касанием, модель `minecraft:translucent`.
* `RadMaterials`: добавлены точечные карты `ITEM_EXACT`/`BLOCK_EXACT`, проверяемые раньше префиксной
  таблицы (иначе `barium_concrete` перехватывался общим префиксом `barium`, а `bore_stained_glass` — `boron`).
* Рецепты выданы в книге рецептов вместе с «Открытием 1» (`RecipeUnlocks`, блок «строительные
  материалы»), физически доступны всегда.
* Локализация: 6 новых ключей в `en_us.json` и `ru_ru.json` (644 = 644).
* Лут-таблицы: `barium_concrete` (обычный дроп + `survives_explosion`), `bore_stained_glass` (silk touch).

Итог по ассетам: `items/` 427 → 433 дефиниции (на момент записи раздела; после ядерного ряда — 444),
все шесть ранее «мёртвых» текстур теперь
используются моделями.

### 8.1. Политика топлива ядерной топки (проверено и уточнено 21.09)

Вопрос автора: «другие штуки типа СО/ТСО, изотопы урана — это всё топка жрёт?»

**Проверка по коду:** единственная точка приёма топлива —
`NuclearFireboxBlockEntity.burnTicks()`, её же используют слот
(`NuclearFuelSlot.mayPlace`), `canPlaceItem` и загрузка через воронку/трубы
(`canPlaceItemThroughFace`). Тегов топлива нет, других путей приёма нет.

| Что | Топка принимает |
|---|---|
| уран: слиток / самородок / блок / пыль / уранинит (`raw_uranium`) | **да** |
| торий: слиток / самородок / блок / пыль / торианит (`raw_thorium`) | **да** |
| `uranium_238/235/233`, `weapons_plutonium`, `plutonium_238/242`, `thorium_229` | **нет** |
| `uranium_fuel`, `mox_fuel`, `tmox_fuel`, `snup_fuel`, `ut_fuel` | **нет** |

Изменения 21.09:

* добавлена **пыль** (`uranium_dust` 900 с, `thorium_dust` 300 с — как слиток, поскольку
  пыль плавится в слиток 1:1); до этого пыль в топку не принималась;
* сырая руда (**уранинит** 500 с / **торианит** 175 с) **оставлена** — решение автора 21.09:
  это тоже природная форма, а не «остальной радиоактивный компонент». Итого топка принимает
  **10 форм**: по 5 на уран и торий.
* приём изотопов и топливных смесей остался запрещён, но теперь это зафиксировано
  явными комментариями в трёх местах: `NuclearFireboxBlockEntity.isNuclearFuel` (javadoc с
  полным списком исключений), `NuclearFuelSlot`, шапка ядерного ряда в `ModItems`
  («заготовки под будущие продвинутые реакторы»).

---

## 9. Разбор пунктов с автором (21.09.2026): решения и статусы

Нумерация — по списку автора. «Фикс» = сделано в этом заходе.

### 9.1. Gradle Wrapper (#1) — **сделано автором 21.09**

Изначально wrapper'а не было (все команды — только локальный `gradle`). Автор закрыл это сам,
коммит `10124e1` («g wrapper»): `gradlew` (248 строк), `gradlew.bat`, `gradle/wrapper/gradle-wrapper.jar`
(47 505 Б), `gradle/wrapper/gradle-wrapper.properties` → **Gradle 9.7.1**. Плагин сборки —
`net.neoforged.moddev 2.0.144` (актуальный; для свежих версий MDG требуется Gradle 9.1+).

Остался один хвост: `gradlew` закоммичен с режимом `100644`, поэтому `./gradlew` отвечает
«Permission denied». В этом заходе бит исправлен (`git add --chmod=+x gradlew` → `100755`),
ждёт коммита. Проверить сборку в песочнице нельзя — Java здесь нет (`sh gradlew --version` →
«JAVA_HOME is not set»); контрольный прогон — на машине автора: `./gradlew build` + `./gradlew selfTest`.

### 9.2. SelfTest — критичное слабое место (#4, #10)

**Почему критично.** Это единственная автоматическая проверка *ядра самой главной механики мода* —
движка формул доски резонанса. Он покрывает ровно те вещи, которые руками не проверишь:
размерностный счёт (`S_D`), числовой счёт (`S_N`), детекция конфликта размерностей,
анти-инфляцию (умножение обеих сторон не должно повышать счёт), серверную авторитетность
(сервер принимает честное преобразование и отвергает подделку/подмену залоченного узла)
и гарантию решаемости головоломок на всех 3 сложностях. Пока тест не запускается, любая правка
`Evaluator`/`GameSolver`/`ChalkboardSubmissionValidator` уходит в игру без проверки — а именно
там живут «ломается через раз» баги.

**Фикс (сделано).** Тест запускается Gradle-таском, Minecraft не поднимается (движок — чистая
Java, 0 импортов `net.minecraft`/`net.neoforged` в `chalkboard/core`):

```
./gradlew selfTest     # wrapper появился 21.09; локально — gradle selfTest
```

Падение любого чека = `exit code 1`. Опционально можно подвесить на `check`:
`tasks.named('check') { dependsOn 'selfTest' }` (в `build.gradle` оставлено комментарием, потому что
`GameSolver.generate()` использует несидированный `Random`, и теоретически возможен редкий флак).

**Что дальше (по желанию):** перенести пакет `chalkboard/core` + тест в отдельный source set
(`engineTest`) и гонять его вообще без Minecraft-зависимостей — тогда тест будет летать за секунды.

### 9.3. Датаген (#14)

#### Что это такое

Датаген (data generation) — механизм NeoForge: `./gradlew runData` поднимает Minecraft-окружение
*без игры*, прогоняет ваши Java-провайдеры и **пишет из них JSON-файлы** (рецепты, лут-таблицы,
теги, блокстейты, модели/дефиниции предметов, lang, worldgen) в `src/generated/resources/`.
Источником правды для ресурсов становится код, а JSON — его артефактом: рецепт «компилируется»
вместе с модом, а не набирается руками в 577-й раз.

#### Что уже готово в проекте

* `build.gradle`: ран `data { data(); --mod gonzotech --all --output src/generated/resources/
  --existing src/main/resources/ }` — настроен полностью; `sourceSets.main.resources { srcDir
  'src/generated/resources' }` тоже есть; в `settings.gradle` препятствий нет;
* `.gitignore`: ингор `src/generated/` **снят автором** (коммит `cf29546` «src gen») — это была
  главная ловушка: без этого вывод датагена не попадал бы ни в репозиторий, ни в сборку у других;
* не хватает **одного класса**: во всём проекте нет ни одного упоминания `GatherDataEvent` /
  `DataGenerator` — то есть датаген пока ни разу не запускался.

#### Что требуется сделать

1. Добавить `com.gonzotech.datagen.ModDataGenerators` (1 файл) и подписать его на mod-шину:

```java
@EventBusSubscriber(modid = GonzoTechMod.MOD_ID, bus = EventBusSubscriber.Bus.MOD)
public final class ModDataGenerators {
    @SubscribeEvent
    public static void gatherData(GatherDataEvent event) {
        DataGenerator gen = event.getGenerator();
        PackOutput out = gen.getPackOutput();
        ExistingFileHelper efh = event.getExistingFileHelper();
        gen.addProvider(event.includeClient(), new ModItemModels(out, efh));      // items/<id>.json
        gen.addProvider(event.includeClient(), new ModLang(out, "en_us"));        // см. ловушку ниже
        gen.addProvider(event.includeClient(), new ModLang(out, "ru_ru"));
        // серверные (теги, лут, рецепты) — добавляются по мере миграции, см. п.3
    }
}
```

   Сигнатуры `PackOutput` / `addProvider(...)` — канон NeoForge 1.21.x (сверять с 21.4.157 при
   первой реализации: провайдеры моделей в 1.21.4 переехали на новый формат `assets/<ns>/items/`).
2. Прогнать `./gradlew runData` сначала **без** провайдеров (создастся
   `src/generated/resources/.cache/` — служебные хэши; их коммитят вместе с ресурсами), затем
   с первым провайдером и убедиться, что `git status` показывает ровно ожидаемые файлы.
3. Порядок миграции (по выгоде/риску):
   * **новый контент** — сразу через провайдеры: блок → `BlockStateProvider` + лут + дефиниция,
     предмет → `basicItem(...)`, новая руда → всё то же плюс теги;
   * **теги (34 файла)** — безопаснее всего: переносим, `runData` должен дать дифф, равный
     содержимому ручных файлов; после сверки ручные удаляем;
   * **lang (644×2 ключа)** — переносим **целиком** и ручные `lang/*.json` удаляем. Иначе в
     сборке окажутся два файла на один путь (`src/generated` и `src/main`), и приоритет будет
     неочевидным. Побочный выигрыш: 63 отсутствующих ключа из 5.1 закрываются одной строкой
     `add(...)` каждый;
   * **лут-таблицы (198) и рецепты (577)** — только по мере правок конкретных файлов: пока для
     них нет провайдера, ручные JSON работают как сейчас, датаген их не трогает и не удаляет.
4. Защита от «забыли перегенерировать»: таск `runData` + `git diff --exit-code` при сборке
   (сначала на машине автора, позже в CI).

#### Альтернатива (если датаген не нужен)

Оставить JSON руками, но закрыть главную боль — отсутствие проверки: один Java-класс
`ResourceValidator` (по реестрам и ресурсам) + таск `./gradlew validateResources`, который падает
со списком, если у зарегистрированного id нет `items/<id>.json`, лут-таблицы/блокстейта, локализации
или если тег/рецепт/модель ссылаются в никуда. Это ровно те ошибки, которые сейчас находились
питоном, и они не требуют миграции 2 446 файлов.

#### Почему не «всё сразу в датаген»

2 446 ручных файлов + невозможность проверить прогон прямо сейчас (в песочнице нет Java) = риск
сломать рабочий проект ради красоты. Пошаговый план даёт выгоду на каждом шаге и ничего не ломает.

### 9.4. Крупный мусор и устаревшие комментарии (#2, #3, #5–#9, #11, #12)

Всё сделано в этом заходе (детали — таблица 6.1):

* удалены: дубликат-папка `advancements/`, 9 legacy `models/item/*_ore.json`, 15 PNG страниц и фона
  заметок, 2 текстуры брони (`textures/models/armor/*` + пустая папка), `hollow_grid_sphere_rotation_32frames.png`,
  мёртвый `LevelRendererSunMixin.java` вместе с его единственным потребителем `SpaceSkyState.getOverworldSunTexture`;
* почищены комментарии: `OreDefinition` (CALCITE-ворлдген теперь существует), `ModItems` (ингредиенты
  давно не placeholder), `machines/package-info` (логистика реализована);
* **(второй заход, вечер 21.09)** ещё 11 комментариев в 8 файлах + 2 строки README — полный
  список и обоснование в 6.3; карта заделов «на будущее» с местами вызова — в 6.4.

### 9.5. Пучки и кориум (#13) — **снято, ложная тревога**

Проверено инструментально (сверка 199 блокстейтов и 444 items-дефиниций); без предметов ровно три —
`composite_pipe`, `second_composite_pipe`, `molten_corium`. Это не «блоки без дефиниции», а блоки
**без предмета вообще**: пучки собираются из труб (СКМ-pick-block выключен), кориум — жидкость
(`LiquidBlock` + `ModFluids`, есть ведро `corium_bucket`, 16×16 PNG флюида, имя
`gonzotech.fluid.molten_corium` — классическое модовое поведение). Никаких `items/*.json` им не нужно.

### 9.6. Ядерные компоненты (#16) — **фикс**

У 11 изотопов/топлив были модели и текстуры, но не было клиентской дефиниции (`assets/gonzotech/items/`).
Добавлены все 11 (по одной строке JSON). Итог: **`items/` 444 дефиниции, 0 зарегистрированных id без
дефиниции**, все model-цели и текстуры резолвятся (проверено скриптом). Заодно перепроверены
добавленные ранее топлива/замесы/бетоны — все джсоны валидны.

### 9.7. Что осознанно НЕ трогали (#17, #18)

* **`solar_panel`** — остаётся во вкладке без рецепта: он относится к «Открытию 4», которое рано или
  поздно начнётся (решение автора).
* **9 dead-end предметов Пресса II** (7 пластин + `redstone_core` + `semiconductor_core`) — остаются:
  после «Открытия 3» под них будут обшивки/корпуса/апгрейды (решение автора). В аудите они помечены
  как «ловушка для игрока до Открытия 3», а не как мусор.
* **2 orphan-текстуры** (`silicon_factory`, `reference_512`) и все прочие «заготовки наперёд» —
  остаются: авторские текстуры рисуются заранее под будущие эпохи (как это и было с барий/бор-замесами).

### 9.8. Источник правды (#15)

Договорённость: **этот аудит — актуальный источник правды** по текущему состоянию проекта: правки
помечаются «(пофикшено)» прямо в тексте, а не переписываются молча. Когда аудит стабилизируется —
на его основе собирается `UPDATED_TEMP_NOTES` (сводный документ «как всё устроено сейчас», вместо
разъехавшихся README + TEMP-notes + docs/TEMP_NOTES).

### 9.9. Состояние на ревизии `cf29546` (вечер 21.09)

* автором добавлены: wrapper (`10124e1`) и снятие игнора `src/generated/` (`cf29546`);
* в этом заходе: exec-бит `gradlew`, 12 комментариев в 9 файлах + 2 строки README (6.3),
  разбор заделов (6.4), актуализация метрик/статусов этого документа;
* **не проверялось в рантайме**: Java в песочнице нет, поэтому после принятия коммита на машине
  автора нужен один прогон `./gradlew build` (+ `selfTest`) — он же первый кандидат на удаление
  149 мёртвых `models/item`-обёрток;
* открытые хвосты: `notes.p15…p40` (46 lang-ключей), рецепты дозиметра/УФ-метра/спидометра,
  `selfTest` не подвешен на `check`, `UPDATED_TEMP_NOTES` ещё не собран, 2 orphan-текстуры
  и 9 dead-end предметов Пресса II — оставлены осознанно (9.7).
