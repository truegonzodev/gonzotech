# AUDIT-2026-09-25 — чистая комната и ресурсы

**Дата:** 25.09.2026 · **Ветка:** `arena/01a0dad3-gonzotech` · **База:** `e5de479` · **Версия:** `0.3.17`
**Статус:** аудит открыт; исправлен только путь текстуры пылемера. Предыдущие аудиты не переписываются.
**Проверка:** статическое чтение кода/ресурсов, `docs/`, истории последних патчей и Python-проверки. `./gradlew compileJava` остановился до сборки: Java/JAVA_HOME отсутствуют. Игровых проверок не было.

## 1. Контекст и последние изменения

Логика проекта: единая физика, сид меняет исследовательский замок, а не КПД; 16 открытий, серверная проверка доски. Эпоха III — точность и управление: кремний → литография → контроллеры → управляемый реактор. Чистая комната — условие производства, **не радиационный контур**. Основа: README, `UPDATED_TEMP_NOTES.md`, планы Эпохи III, `DEFERRED-2026-09-22.md`, спецификация §5.3 в `EPOCH3-LITHOGRAPHY-PLAN-2026-09-26.md`; GUI — `SUPER_CRUCIAL_FILE.md`.

История: `f8a0ad3` — первая реализация; `6ab3c65` — узлы в оболочке; `1212ffe` — внутренние препятствия, но вместе с этим удалена проверка оболочки; `26f4f73` — старт **100%**; `5011ad0` — меню; `4f6bb46` — переименование в `third_`; `77b56d2`/`e6298f8` — цвет телифона/вкладка пылемера.

**Не откатывать вслепую:** 100% — явное позднее изменение, не случайная ошибка инициализации. Спецификация всё ещё требует 0% и сброс после разгерметизации; её шапка «кодирование ещё не начато», README «в очереди» и DEFERRED «ждём описания» устарели. Сначала согласовать актуальный контракт. Литография/production fallback 10% пока остаются планом, не готовой механикой.

## 2. Актуальные дыры

P1 — ломает основную механику/сохранность; P2 — интеграция, интерфейс, эксплуатационные риски. Строки относятся к базе аудита.

| № | Приоритет | Дыра и следствие | Где |
|---|---|---|---|
| 1 | **P1** | **Одна комната получает разные состояния.** FNV-хеш считается в порядке BFS, зависящем от начальной клетки. Игрок, второй игрок и фильтр меняют разные quality; шаг в сторону может дать новые 100%. Нужен общий идентификатор объёма, независимый от точки запроса. | [CleanRoomDetector:18–43](../src/main/java/com/gonzotech/cleanroom/CleanRoomDetector.java#L18), [CleanRoomSystem:55–63,102–106](../src/main/java/com/gonzotech/cleanroom/CleanRoomSystem.java#L55) |
| 2 | **P1** | **Нет жизненного цикла комнаты.** Статическая `ROOMS` не инвалидируется, не сохраняется и не очищается; `topologyTick` не используется. Восстановленный прежний объём возвращает старое качество; перезапуск процесса теряет его, а смена миров в одном процессе допускает перенос между одинаковыми координатами/измерениями. | [CleanRoomSystem:20–30,39–40,55–57](../src/main/java/com/gonzotech/cleanroom/CleanRoomSystem.java#L20) |
| 3 | **P1** | **Любая небольшая полость — clean room.** Обход различает только air/non-air: земля, камень, растения и обычная открытая дверь могут замыкать объём. Допустимые материалы/clean-room tag отсутствуют. Нужно отличать внешнюю оболочку от внутренних препятствий; особое правило гермодвери сохранить отдельно. | [CleanRoomDetector:19–43](../src/main/java/com/gonzotech/cleanroom/CleanRoomDetector.java#L19), регрессия `1212ffe` |
| 4 | **P1** | **Фильтр расходует неверно и работает впустую.** Счётчики растут раз в секунду, пороги 3600/10800: уголь/Pt/Pd расходуются через **60/180 минут**, не секунд. GTU и время расходников списываются до проверки комнаты. Проверяется только `pos.above()` — фильтр под потолком не очищает соседний воздух. | [AirFilterBlockEntity:39–49](../src/main/java/com/gonzotech/cleanroom/AirFilterBlockEntity.java#L39), [CleanRoomSystem:102–106](../src/main/java/com/gonzotech/cleanroom/CleanRoomSystem.java#L102) |
| 5 | **P1** | **Неполный дроп.** У `third_air_cleaner`, `third_air_filter`, `aluminum_housing`, `sheathing` нет loot tables и mineable-тегов при `requiresCorrectToolForDrops()`. У фильтра также нет обработчика выпадения содержимого при удалении. Одной loot table для сохранности загруженных предметов недостаточно. | [ModBlocks:106–126](../src/main/java/com/gonzotech/core/registry/ModBlocks.java#L106), [AirFilterBlock](../src/main/java/com/gonzotech/cleanroom/AirFilterBlock.java), `data/gonzotech/loot_table/blocks`, `data/minecraft/tags/block` |
| 6 | **P2** | **Очиститель не соответствует геометрии/импульсу.** Поиск только в колонке `BlockPos` игрока; dx/dz всегда 0, соседняя колонка в пределах 0.7 не учитывается. Вне валидной комнаты поток вообще не снимает грязь. Любое соседское обновление под питанием перезапускает 160 тиков, фронт сигнала не отслеживается. | [CleanRoomSystem:39–45,70–86](../src/main/java/com/gonzotech/cleanroom/CleanRoomSystem.java#L39), [AirCleanerBlock:24–27](../src/main/java/com/gonzotech/cleanroom/AirCleanerBlock.java#L24) |
| 7 | **P2** | **GUI подключён не по формату арта.** Лист 512×512 читается как 176×184 от (0,0); рисуется только `_bg`, передний слой со слотами не используется. Координаты `AirFilterMenu` не совпадают с PNG; синхронизации/индикации GTU нет. Это не missing texture, а ошибка отрисовки и раскладки. | [AirFilterScreen:13–29](../src/main/java/com/gonzotech/machines/client/AirFilterScreen.java#L13), [AirFilterMenu:20–34](../src/main/java/com/gonzotech/machines/menu/AirFilterMenu.java#L20) |
| 8 | **P2** | **Survival-интеграция не завершена:** у очистителя, фильтра и пылемера нет рецептов; машины отсутствуют в выдаче рецептов/физическом гейте III. Это пока недоступный survival-контент, а не обход существующего рецепта. Отдельно: `fce1ca7` убрал все три двери из физического гейта, хотя README обещает блокировку — подтвердить решение. | `data/gonzotech/recipe`, [TierThreeCrafting:49–80,130–140](../src/main/java/com/gonzotech/machines/crafting/TierThreeCrafting.java#L49) |
| 9 | **P2** | **Сохранение энергии:** `receiveGtu()` меняет буфер без `setChanged()`. Неработающий фильтр может зарядиться, но не пометить чанк для сохранения. Проверить зарядку без расходников → сохранение/выгрузка → загрузка. | [AirFilterBlockEntity:68,87–88](../src/main/java/com/gonzotech/cleanroom/AirFilterBlockEntity.java#L68) |
| 10 | **P2** | **Повторные обходы без защиты от загрузки чанков.** Каждый игрок раз в секунду, прибор и работающий фильтр запускают BFS до 2049 клеток; нет общего кэша и проверки загруженности перед `getBlockState`. Нужны бюджет/инвалидация и тест у границы загруженных чанков. | [CleanRoomDetector:18–36](../src/main/java/com/gonzotech/cleanroom/CleanRoomDetector.java#L18), вызовы в `CleanRoomSystem` |

**Минимальные подтверждения без Minecraft:** перенос алгоритма BFS/FNV в Python для двух клеток `(0,64,0)` и `(1,64,0)` дал разные хеши `090bdb47b5a4a6ed` / `08537dc7b5a4a6ed`; восстановление того же набора клеток возвращает прежний ключ. Модель счётчиков дала первое списание на 3600/10800-й секунде. Игрок X=1.1 относительно очистителя с центром X=0.5 находится в радиусе 0.7, но выпадает из поиска по колонке. Это проверки алгоритма, не игровые тесты.

## 3. Пути текстур/JSON и результаты сканов

- **Исправлено:** `textures/item/components/dust_meter.png` → `textures/item/dust_meter.png`; `models/item/dust_meter.json` теперь ссылается на `gonzotech:item/dust_meter`. Байты PNG сохранены, старого файла/ресурсных ссылок нет. Реальное имя — `dust_meter`, id предмета не менялся.
- **Битых проверенных ссылок не найдено:** 2525 JSON парсятся; дополнительно проверены вложенные item/blockstate model refs, parents, текстуры моделей, equipment, particles, 31 `.mcmeta`, 101 Java PNG-ссылка (включая `gui()`/шаблоны заметок). Ванильные пути не принимались за отсутствующие ресурсы мода. Проверка существования не заменяет загрузку ресурсов игрой.
- **Хвост переименования:** 10 старых файлов `air_cleaner`/`air_filter` в blockstates/items/models/textures дублируют `third_`-версии; старые id уже не зарегистрированы. Их надо чистить, а не добавлять им локализацию/лут. Плейсхолдеры и согласованные опечатки `corium_bukket`/`sculk_bircks` не объявляются битыми путями.
- **Локализация:** en/ru по 893 ключа, но `DustMeterItem:22–28` выводит русские литералы при любом языке; телифон уже использует lang.

| Проверка | Итог |
|---|---|
| `python3 audit/validate.py` | **8 ошибок**, 0 предупреждений: 4 от мёртвых старых blockstates (2 lang + 2 loot), 4 реально отсутствующих loot tables (§2.5). До/после переноса одинаково. |
| `python3 audit/naming_scan.py` | **4 расхождения**: 2 новых машины без гейта; 2 ложных вывода о физическом гейте корпусов — скрипт смешивает `RECIPE_IDS` (видимость) и `isGatedOutput` (крафт). Корпуса переименовывать не нужно. |
| `python3 audit/doc_links.py` | **0 битых ссылок**. |
| `./gradlew compileJava` | **Не выполнена:** нет Java; сборка, рендер блоков/GUI и multiplayer не подтверждены. |

## 4. Ближайшая приёмка

1. Утвердить стартовые 0/100% и правила сброса; исправить общий room id, оболочку и жизненный цикл.
2. Проверить два игрока + фильтр в одной комнате; шаг/телепорт внутри; разбор/восстановление; обычную и гермодверь; перезапуск/смену мира.
3. Проверить фильтр за 60/180 секунд, без контура, под потолком; очиститель на границе радиуса и при удержанном сигнале; дроп полного фильтра, сохранение GTU; затем GUI, рецепты/гейты и оба языка.

## 5. Журнал сессии

| Дата | Патч | Статус |
|---|---|---|
| 25.09.2026 | Краткий аудит + перенос PNG пылемера и обновление модели | Только ресурсный путь; игровая логика/баланс и версия не менялись. Без PR. |
